# REFATRIX 인수인계 2026-09-18 — 프로모션 품목을 **제품마스터(PRO)** 로 등록

**마이그레이션 없음** (`npm run migrate` 불필요) · **백엔드 1 + 프런트 1 + 테스트 4**
**산출물 zip**: `refatrix_promo_products_v1.zip`
라우트 rev `20260827spot2` → **`20260918promo`** · 프런트 build `sc0827spot2` → **`sc0918promo`**
베이스: 2026-09-18 라이브 HEAD (편집 직전 cache-busted raw URL 로 재수신)

---

## ① 설명 — 무엇을, 왜

### 디렉터 지시 (2026-09-18)
> "제품조회에도 나와야 해. 이건 제품목록에 반영되어야 하는 거야. PRO가 반드시 붙어야 하고 숫자는 현재 등록된 번호 연속으로 자동제안해줘."

### 지금까지의 구조와 문제
프로모션 품목은 `promo_items` 라는 **별도 테이블**에 있었다. 자동차부품 마스터(`products`)와 분리해
견적·매출을 오염시키지 않으려는 설계였는데, 그 대가로 **제품조회에 아예 안 나오고 견적에서도 못 쓴다.**
창고 화면에서 수량을 직접 써넣는 방식이라 **이동내역(원장)도 남지 않았다.**

### 이번에 바꾼 것
신규 프로모션 품목은 **`products` 에 `PRO` 코드로 등록**된다. 그래서:

| | 전 | 후 |
|---|---|---|
| 저장 위치 | `promo_items` | **`products`** (코드 `PRO001`…) |
| 제품조회 | 안 나옴 | **나옴** — 「🎁 마케팅 상품(PRO)」 필터로 바로 걸림 (화면 변경 없음, 이미 있던 필터) |
| 견적·매출 | 불가 | **가능** — 견적 제품검색에 잡히고 고객 할인율·IVA 그대로 적용 |
| 코드 | 자유 입력(`PROMO-GUANTE`) | **`PRO` + 연속번호 자동제안**(3자리, `PRO014`) · 수정 가능하되 `PRO` 필수 |
| 수량 | 화면에서 직접 덮어씀(이력 없음) | **이동내역(조정)을 통해서만** — 등록 시 초기수량 1건, 이후 [수량조정] 또는 전체 재고실사 반영 |
| 재고실사 | `promo` 라인으로 별도 처리 | 일반 **부품과 동일하게** 스캔·대조·반영(PRO 도 `products` 이므로) |

### 코드 자동제안 규칙
- `products` 에서 **`PRO`+숫자** 형태만 번호로 본다 (`PRO-CAP`·`PROGORRA` 같은 문자 코드는 번호 계산에서 제외).
- 최댓값 + 1 에서 시작해 **이미 쓰인 번호는 건너뛴다.** 삭제된 제품도 코드 UNIQUE 를 점유하므로 같이 본다
  (예: `PRO013` 까지 있고 `PRO014` 가 삭제행이면 → **`PRO015`** 제안).
- 자릿수 3 (`PRO001`). 999 를 넘으면 자연스럽게 4자리가 된다.

### 수량이 원장을 거치는 이유 (중요)
`products.stock_qty` 는 이 시스템에서 **어느 화면에서도 직접 편집할 수 없는 값**이다(입고·매출·실사 반영만 바꾼다).
프로모션이라고 예외를 두면 "재고 숫자와 이동내역이 안 맞는 제품"이 생긴다. 그래서:

- **초기수량** → `stock_movements` 에 `adjust` 1건 (`ref=promo:init:PRO015`, 비고 `프로모션 품목 초기등록`)
- **수량조정** → 목표수량과의 차이를 `adjust` 1건 (`ref=promo:adjust:PRO015`, 비고 `프로모션 수량조정 · <사유>`) · **사유 필수**
- 즉 **원장 합계 = 현재 재고**가 항상 성립한다 (테스트가 이 불변식을 고정).

### 권한 — 왜 창고가 제품을 만들 수 있게 했나
제품마스터 신규 등록은 원래 **디렉터 전용**이다. 프로모션 품목까지 디렉터를 거치게 하면 현장이 멈추므로,
**`PRO` 접두사 코드에 한해서만** 창고(warehouse) 편집권한으로 열었다.
등록·수정·수량조정 **세 경로 전부** 서버에서 `PRO` 코드인지 확인하고, 아니면 `not_promo_product` 로 거부한다
(부품 마스터는 이 경로로 절대 바뀌지 않는다 — 정적 가드 테스트가 고정).

### 구 `promo_items` 는 그대로 둔다 (디렉터 선택: "신규만 products에 등록")
- 기존 항목은 화면 아래쪽 **「구 프로모션 품목 · 제품마스터 미등록」** 표로 내려가고, 편집·삭제는 종전대로 된다.
- 다만 **제품조회·견적에는 계속 안 나온다.** 필요하면 새 [＋ 품목 추가] 로 다시 등록하고 구 항목을 지우면 된다
  (⑧ 다음 액션 참고 — **9/18 에 등록하신 그 품목이 여기 해당**).
- 구 표에서는 **신규 생성이 막혀 있다**(위 [＋ 품목 추가] 로 유도).

---

## ② 배포 단계 — ⚠ 순서 준수 (마이그레이션 없음)

1. `refatrix-api/src/routes/stockCountRoutes.js` 덮어쓰기 → GitHub Desktop **Commit / Push**
2. Railway 재배포 **Success** 확인 → 로그에 `[stockCountRoutes] loaded rev 20260918promo`
   - **`npm run migrate` 불필요** (새 테이블·컬럼 없음)
3. `refatrix-stockcount.html` 덮어쓰기 → Push → Pages 1~2분
4. 재고실사 화면에서 **`Ctrl+Shift+R`** → 콘솔 `[refatrix-stockcount] build sc0918promo`
5. (선택) 테스트 4개 파일도 함께 push

> **반쪽 배포 주의**: 프런트가 새 API(`/api/promo-products`)를 부르므로 **백엔드를 먼저** 올려야 한다.
> 순서가 바뀌면 프로모션 화면이 "불러오기 실패" 토스트만 띄운다(다른 화면은 정상).
> `refatrix-nav.js` 무변경 → **캐시버스터 일괄 bump 불필요**.

### 배포 검증
```
B=https://raw.githubusercontent.com/sebastianham26-art/Refatrix/main
curl -s "$B/refatrix-api/src/routes/stockCountRoutes.js?nc=$(date +%s)" | grep -c "20260918promo"     # 2
curl -s "$B/refatrix-api/src/routes/stockCountRoutes.js?nc=$(date +%s)" | grep -c "promo-products"    # 5 이상
curl -s "$B/refatrix-stockcount.html?nc=$(date +%s)" | grep -c "sc0918promo"                          # 4
```

---

## ③ 테스트 방법 (현장 스모크 5분)

1. 창고 › 재고실사 › **🎁 프로모션 품목 관리** → 표가 **둘**로 보이는지 (위: 제품마스터 PRO / 아래: 구 프로모션 품목).
2. **[＋ 품목 추가]** → 코드칸에 **다음 번호가 자동으로 채워지는지** (예: `PRO014` · 안내문 "자동 제안 …").
3. 품명·바코드·랙·**판매정가**·초기수량을 넣고 저장 → 목록에 바로 나타나는지.
4. ★ **제품조회 화면** → 「🎁 마케팅 상품(PRO)」 필터를 **「PRO만」** 으로 → 방금 만든 품목이 **재고수량과 함께** 보이는지.
5. ★ **견적 화면** → 제품검색에 `PRO` 를 치면 **자동완성에 뜨고**, 담으면 판매정가 × 고객할인으로 금액이 잡히는지.
6. **재고 › 이동내역** → 방금 등록분이 **조정(adjust)** 1건으로 남아 있는지 (비고 `프로모션 품목 초기등록`).
7. **[수량조정]** → 사유를 비우고 저장 → **막히는지**. 사유를 넣고 저장 → 수량이 바뀌고 이동내역이 1건 더 생기는지.
8. `PRO` 로 시작하지 않는 코드(예: `CE9999`)로 등록 시도 → **거부되는지**.
9. 회귀: 전체 재고실사 1건을 열어 기록·대조·반영이 **예전 그대로**인지, SKU 스팟점검도 종전대로인지.
10. 회귀: 구 프로모션 품목 1건을 편집·저장 → 종전대로 되는지.

---

## ④ 변경 파일

| 파일 | repo 경로 | 종류 | 변경 요약 |
|---|---|---|---|
| `stockCountRoutes.js` | `refatrix-api/src/routes/stockCountRoutes.js` | 수정 | 프로모션 제품(PRO) API 5종 추가(`next-code`·목록·등록·수정·수량조정) · rev `20260918promo` (**+약 200줄**, 기존 로직 무변경) |
| `refatrix-stockcount.html` | `refatrix-stockcount.html` (repo 최상위) | 수정 | 프로모션 화면 2표 구조 · 코드 자동제안 모달 · 수량조정 모달 · build `sc0918promo` |
| `promo_products_sql.test.mjs` | `refatrix-api/test/promo_products_sql.test.mjs` | **신규** | 정적 가드 + 운영 SQL 대조 + pg-mem 런타임 40건 |
| `promo_ui.test.js` | `test/promo_ui.test.js` | **신규** | jsdom UI 60건 |
| `stock_spot_sql.test.mjs` | `refatrix-api/test/stock_spot_sql.test.mjs` | 수정 | rev·build 마커 갱신 · `stock_qty` UPDATE 개수 가드 1 → 3 |
| `spot_ui.test.js` | `test/spot_ui.test.js` | 수정 | build 태그 마커 갱신 |

- **마이그레이션 없음.** `refatrix-nav.js` 무변경 → 캐시버스터 bump 불필요. `server.js` 무변경(기존 라우트 파일에 얹음).

### 새 API

| 메서드 | 경로 | 권한 | 내용 |
|---|---|---|---|
| GET | `/api/promo-products` | warehouse 읽기 | PRO 제품 목록(코드·품명·EAN·랙·판매정가·재고·활성) |
| GET | `/api/promo-products/next-code` | warehouse 읽기 | 다음 코드 제안 (`PRO014`) |
| POST | `/api/promo-products` | warehouse 편집 | 등록 + 초기수량(조정 이동) |
| PATCH | `/api/promo-products/:id` | warehouse 편집 | 품명·EAN·랙·판매정가·IVA·SAT·활성 (**수량 불가**) |
| POST | `/api/promo-products/:id/adjust` | warehouse 편집 | 수량조정(목표수량 + **사유 필수**) |

---

## ⑤ 검증 결과 (이번 세션 — **226 / 226 ✅**)

- **ESM 문법** — `node --input-type=module --check` (백엔드) · HTML 인라인 스크립트 전 블록 `node --check` ✅
- **pglast v8.4** — 신규 SQL 14종 전부 파싱 통과 (`${}` 보간은 리터럴 치환 후 검사)
- **`refatrix-api/test/promo_products_sql.test.mjs` 40/40** (pg-mem)
  - 정적 가드: PRO 가드 3경로 · 권한 게이트 · **`stock_qty` UPDATE 3곳 ↔ `stock_movements` INSERT 3곳(1:1)** ·
    스팟 블록 재고 불변(회귀) · 감사 액션이 0057 CHECK 범위
  - **운영 SQL 대조** — 테스트가 실행하는 SQL 6종이 운영 소스에 **문자 그대로 있는지 먼저 확인**(드리프트 방지)
  - 런타임: `PRO001` 시작 · `PRO013 → PRO014` · **삭제된 `PRO014` 를 피해 `PRO015`** · 문자형 `PRO-CAP` 무시 ·
    초기수량 30 이 재고 + 이동 1건 · 목록에 부품 미포함/삭제행 제외 · 조정 30→12 가 −18 이동으로 · **원장 합계 = 재고** · 부품코드 거부
- **`test/promo_ui.test.js` 60/60** (jsdom, 운영 HTML 인라인 스크립트를 그대로 실행)
  - 코드 자동 채움 · 품명/PRO 접두사 미충족 시 **요청 자체가 안 나감** · POST 페이로드(코드·수량·판매가·IVA·SAT·원가) ·
    편집은 PATCH 이고 **본문에 `stock_qty` 가 없음** · 수량조정 사유 필수·음수 차단 · 판매정가 없으면 「미설정」 경고 ·
    구 promo_items 는 PATCH 만 · 실사 화면 불변
- **기존 스위트 회귀** — `stock_spot_sql.test.mjs` 23/23 (DB 파트는 `TEST_PG_URL` 없어 skip) · `spot_ui.test.js` **103/103**
- **라이브 HEAD 기준 편집** — 편집 직전 cache-busted raw URL 로 두 파일 재수신(stale 덮어쓰기 방지)

> 실 PostgreSQL 종단 테스트는 이번 범위에서 돌리지 않았다(스키마 변경이 없고, 쓰는 SQL 이 기존 실사 반영 경로와 동일 형태).
> 배포 후 ③-6(이동내역 확인)이 그 자리를 대신한다.

---

## ⑥ 결정 사항 (디렉터 확인 2026-09-18)

| 항목 | 결정 |
|---|---|
| 데이터 구조 | **신규만 `products` 에 등록.** 기존 `promo_items` 는 이관하지 않고 화면에서 구분 표시 |
| 영업 노출 | **견적·매출에서도 선택 가능** (제품조회에만 노출하는 안 대신) |
| 초기 수량 | **재고조정 이동내역으로 즉시 반영** (수량 0 등록 후 실사로만 채우는 안 대신) |
| 코드 형식 | **`PRO001` 3자리**, 기존 최대번호 다음을 자동 제안 |
| 권한 | PRO 코드에 한해 **창고 편집권한**으로 제품 등록 허용 (그 외 제품마스터는 종전대로 디렉터 전용) |
| 참고 원가 | 등록 시 입력한 원가는 **초기 등록 1회만** `avg_cost` 에 넣고 이동내역에도 기록. 이후 자동 재계산 대상 아님(판촉물은 수입배치를 안 탐) |

---

## ⑦ 오픈 이슈 / 확인 필요

1. **★ 재고 요약 카드의 PRO 제외 룰 — 디렉터 판단 필요.**
   2026-07-08 에 "PRO 는 판매용이 아니다"라는 이유로 **이동내역 요약 카드**(현재 SKU 수·총 수량·재고금액·판매가 환산액)와
   **PSN 재고 전월비교**에서 PRO 를 제외해 두었다. 이제 PRO 를 **판매 가능**으로 바꿨으므로 전제가 달라졌다.
   - **이번 패키지는 그 룰을 그대로 두었다**(제외 유지). 바꾸면 재고금액·WBR 숫자가 즉시 움직이기 때문에 조용히 바꾸지 않았다.
   - 포함시키려면 `stockRoutes.js` `/api/stock/summary` 의 `AND code NOT ILIKE 'PRO%'` 한 줄(+PSN `net_cur` 서브쿼리)만 지우면 된다. **지시 주시면 별건으로 처리.**
2. **SAT 코드** — 판촉물은 부품과 SAT 분류가 다르다. 비워 두면 청구서 발행 단계에서 막힐 수 있다. 회계사에게 티셔츠·모자·장갑용 코드를 한 번 받아 두시면 등록이 깔끔해진다.
3. **판매정가 미설정** — 목록에 「미설정」 경고를 띄우지만 저장은 막지 않았다(사은품으로만 쓸 수도 있어서). 견적에 담으면 금액 0 으로 잡힌다.
4. **부족분·오퍼시트** — PRO 제품도 이제 견적 대상이라, 재고보다 많이 담으면 부족분 기록이 생길 수 있다. 판촉물이 고객 오퍼시트에 섞이면 알려 주시면 PRO 제외를 넣겠다.
5. **구 `promo_items` 이관** — 자동 이관은 하지 않았다(디렉터 결정). 건수가 적으면 손으로 다시 등록하는 편이 안전하고, 많아지면 이관 스크립트를 별건으로 만들 수 있다.

---

## ⑧ 다음 액션

1. ② 순서대로 배포 → ③ 스모크.
2. ★ **9/18 에 등록하신 그 프로모션 품목은 구 `promo_items` 에 있습니다.** 배포 후
   [＋ 품목 추가] 로 **다시 등록**하고(수량 동일하게), 아래 「구 프로모션 품목」 표에서 원래 항목을 **삭제**하세요.
   두 군데 남겨 두면 재고실사에서 같은 물건이 두 줄로 잡힙니다.
3. ⑦-1 (재고 요약에 PRO 포함할지) 판단 → 지시 주시면 한 줄 변경 배포.
4. (선택) 판촉물 SAT 코드 확보 후 각 품목에 채워 넣기.

### 이월 (직전 유지)
- 스팟점검 「틀림」 건을 전체 재고실사 세션으로 넘기는 동선 — 디렉터 판단 대기
- 전체실사에서 카톤 라벨 스캔 시 수량을 소입수로 자동 채울지 — 디렉터 판단 대기
- 기존 백로그: 구매제안 엔진, CFDI de egreso 2단계, WhatsApp Business API, 랙 위치 엑셀 매핑, 전 SKU COGS 재계산, pgAdmin 로컬 백업

---
*이 문서는 인수인계용 스냅샷입니다. 큰 변경이 생기면 갱신해서 다시 올려주세요.*
