# REFATRIX 핫픽스 2026-09-22 — 프로모션 화면에 **작업 버튼 노출 + 실사 반영 대기 표시**

**프런트 1 파일** · 백엔드·마이그레이션 없음 · nav.js 무변경(캐시버스터 bump 불필요)
build `sc0918promo` → **`sc0922promo2`**
**산출물 zip**: `refatrix_promo_apply_v1.zip`
베이스: 2026-09-22 라이브 HEAD (편집 직전 cache-busted raw URL 로 재수신)

---

## ① 설명 — 무엇을, 왜

### 디렉터 지적
> "프로모션 품목 화면에는 적용하는 버튼이 없어"

### 원인 (라이브 화면에서 확인)
1. **버튼이 화면 밖에 있었다.** [편집 · 수량조정] 링크가 표의 **맨 오른쪽 열**에 있는데, 품명이 길고
   (`material promocion_…`) 줄바꿈이 막혀 있어(`white-space:nowrap`) 표가 가로로 넘친다.
   → 표 안을 **옆으로 스크롤해야만** 링크가 보였다. 데스크톱(1510px)에서도 안 보였다.
2. **실사 결과를 반영하는 길이 이 화면과 연결돼 있지 않았다.** 전체 재고실사로 센 PRO 수량은
   재고실사 목록 › [대조 ▸] › 🔒 실물 반영 검토(디렉터 PIN) 로만 반영된다. 프로모션 화면에서는 어떤 실사가
   반영을 기다리는지 알 수 없었다.

### 바꾼 것
| | 전 | 후 |
|---|---|---|
| 작업 버튼 | 맨 오른쪽 작은 링크 — 가로 스크롤해야 보임 | **맨 앞 열 버튼 [⚖ 수량조정] [편집]**, 가로 스크롤해도 **고정(sticky)** |
| 품명 | 한 줄 고정(표를 넓힘) | 줄바꿈 허용 |
| 열 순서 | 코드·품명·EAN·랙·판매정가·재고 | **작업·코드·품명·재고수량·실사 반영 대기**·EAN·랙·판매정가 |
| 실사 연결 | 없음 | 품목마다 **「SC-2026-0011 · 실사 18 (+2) [대조·반영 ▸]」** — 누르면 그 실사의 대조 화면으로 이동 |
| 상단 안내 | 없음 | 「⏳ 반영 대기 중인 재고실사 N건에 프로모션 품목이 들어 있습니다」 |
| 구 프로모션 표 | 같은 문제 | 같은 방식(맨 앞 [편집] + 실사 대기 열) |

- **반영 대기** = `status=submitted` 이고 `mode=full` 인 실사의 라인을 품목별로 합산한 값. 스팟점검·작성중·반영완료 세션은 읽지 않는다.
- **재고는 이 화면에서 바뀌지 않는다.** 화면은 GET 만 부른다. 반영은 여전히 대조 화면 → 🔒 실물 반영 검토 → 디렉터 PIN (권한·감사 경로 불변).
- 인라인 `onclick` 대신 `data-act` + 위임 리스너(`addEventListener`) — 화면 재진입 시 중복 바인딩 없음.

---

## ② 배포 단계

1. `refatrix-stockcount.html` 덮어쓰기 → GitHub Desktop **Fetch/Pull → Commit / Push**
2. Pages 1~2분 → 재고실사 화면 **`Ctrl+Shift+R`** → 탭 제목 `build sc0922promo2`
3. (선택) 테스트 4개 파일 함께 push

```
B=https://raw.githubusercontent.com/sebastianham26-art/Refatrix/main
curl -s "$B/refatrix-stockcount.html?nc=$(date +%s)" | grep -c "sc0922promo2"   # 6
```

---

## ③ 테스트 방법 (현장 2분)

1. 창고 › 재고실사 › **🎁 프로모션 품목 관리** → 각 행 **맨 앞에 [⚖ 수량조정] [편집]** 버튼이 보이는지.
2. 표를 옆으로 밀어도 버튼 열이 **왼쪽에 붙어 있는지**.
3. 상단에 「⏳ 반영 대기 중인 재고실사 3건」 안내가 보이는지 (9/22 현재 SC-0010·0011·0012).
4. PRO015 행에 `SC-2026-0011 · 실사 18 (+2)` → **[대조·반영 ▸]** → 대조 화면이 열리고 **🔒 실물 반영 검토** 버튼이 있는지(디렉터).
5. PRO_019 행에 `SC-2026-0010 · 실사 88 (+88)` 이 보이는지 — ⑦-1 참고.
6. 회귀: [수량조정] 사유 없이 저장 → 막힘 / 사유 입력 → 수량·이동내역 반영. [＋ 품목 추가] 코드 자동제안.

---

## ④ 변경 파일

| 파일 | repo 경로 | 종류 | 변경 요약 |
|---|---|---|---|
| `refatrix-stockcount.html` | `refatrix-stockcount.html` (repo 최상위) | 수정 | 프로모션 두 표 열 재배치·작업열 sticky·품명 줄바꿈 · `loadPromoPending`/`pendCell`/`renderPendBar`/`bindPromoActs` 추가 · build `sc0922promo2` |
| `promo_pending_ui.test.js` | `test/promo_pending_ui.test.js` | **신규** | jsdom 28건 |
| `promo_ui.test.js` | `test/promo_ui.test.js` | 수정 | build 마커만 |
| `spot_ui.test.js` | `test/spot_ui.test.js` | 수정 | build 마커만 |
| `stock_spot_sql.test.mjs` | `refatrix-api/test/stock_spot_sql.test.mjs` | 수정 | build 마커만 |

---

## ⑤ 검증 결과

- HTML 인라인 스크립트 2블록 `node --check` ✅
- **`test/promo_pending_ui.test.js` 28/28** — 작업열 첫 칸·sticky·인라인 onclick 0 · 버튼 → 기존 수량조정/편집 모달 · 재진입 후 리스너 1회 ·
  운영 데이터 형태로 대기 계산(PRO015 +2, PRO016 한 세션 2줄 합산=일치, PRO_019 +88, 구 PRO200) · 스팟/작성중/반영완료 세션 제외 ·
  **GET 외 호출 0** · [대조·반영 ▸] → 해당 세션 reconcile + 화면 전환 · 대기 없을 때 안내 비움
- 기존 회귀 — `promo_ui.test.js` **60/60** · `spot_ui.test.js` **103/103**
- **실제 Chromium 렌더** 1510×812 · 360×640 — 수량조정 버튼이 표 영역 안(`getBoundingClientRect`), 표를 끝까지 가로 스크롤한 뒤에도 안, 페이지 가로 스크롤 0
- 백엔드·SQL 변경 없음 → pglast/pg-mem 대상 없음

---

## ⑥ 결정 사항
- 반영 권한·경로는 **바꾸지 않았다**(디렉터 PIN 유지). 프로모션 화면은 "어디서 반영하면 되는지" 연결만 한다.
- 대기 수량은 서버 신규 API 없이 기존 `/api/stock-counts`·`/api/stock-counts/:id` 로 계산(제출 세션 수가 적어 요청 수 문제 없음).

## ⑦ 오픈 이슈
1. **★ 가방 이중 등록** — `PRO_019 Bolsa CTR`(재고 0) · `PRO019 Bolsa CTR`(재고 77). SC-2026-0010 은 PRO_019 로 88개를 셌다.
   그대로 반영하면 가방이 77+88 로 이중 계상된다. 권장: SC-0010 반영 검토에서 PRO_019 **보류** → PRO019 [수량조정] 88(사유 「실사 SC-2026-0010」) → PRO_019 비활성.
2. PRO 코드 형식 가드(`PRO` + 숫자만 허용, `PRO_019` 같은 형식 거부) — 지시 시 백엔드 별건.
3. 창고 담당자에게 반영 권한을 줄지 — 현재 디렉터 전용.
4. 구 프로모션 `PRO200 테스트` 999개 — 테스트 데이터로 보임, 삭제 권장.

## ⑧ 다음 액션
1. ② 배포 → ③ 확인.
2. SC-2026-0012: 대조 → 실물 반영 검토 → 「반영완료로 마감」(차이 0).
3. SC-2026-0011: PRO015 +2 반영.
4. SC-2026-0010: ⑦-1 순서대로.

---
*이 문서는 인수인계용 스냅샷입니다. 큰 변경이 생기면 갱신해서 다시 올려주세요.*
