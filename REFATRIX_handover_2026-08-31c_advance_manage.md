# REFATRIX 인수인계 — 선수금(과입금) 관리 화면: "반제가 사라지고 선수금으로만 보인다"의 해결

- 작업일: 2026-08-31 (같은 날 세 번째 — `bank_deposit_partial_alloc` · `ar_settled_date` 에 이어서)
- **마이그레이션 없음** (0191 은 첫 작업 것 — 같은 zip 에 함께 들어있음)
- 변경 파일: 백엔드 1(`financeRoutes.js`) · 프런트 1(`refatrix-settlement.html`) · 테스트 1(신규)
- **nav 캐시 마커**: `v20260831bd` (앞 작업들과 동일)
- 산출물 zip: **`refatrix_20260831_bd_partial.zip`** — 세 작업이 같은 파일을 공유하므로 **한 zip 으로 배포**
- 검증: 실 PostgreSQL 16 · **종단 9건 전부 통과**(3회 연속 재실행 포함) · 전체 스위트 회귀 **0건**

---

## ① 증상 / 원인

**증상(디렉터 보고)**: "반제 처리한 게 사라진 게 있다. 현재 상태는 **선수금 과입금**으로 보이고 있다. **선수금을 어디서 삭제하나?**"

**답: 삭제할 수 있는 화면이 없었다.** 코드 전수 확인 결과:

| 화면 | 선수금이 보이나 | 지울 수 있나 |
|---|---|---|
| 재무 > **거래목록** | 보인다 (`kind='advance'` → 「선수금」 라벨) | **불가** — `DELETE /api/transactions/:id` 가 `kind!=='general'` 이면 `kind_not_deletable` 로 거부. 수정도 `sales_linked_readonly` |
| 수금/정산 > **수금내역 드릴다운** | **안 보인다** | 그 [삭제]는 *배분(allocation)* 삭제 버튼인데, 선수금만 남은 입금건은 **배분이 0건**이라 어떤 인보이스 드릴다운에도 나타나지 않는다 |
| API `DELETE /api/ar/payments/:id` | — | 존재하고 정상 동작하지만 **호출하는 화면이 하나도 없었다**. 고객별 입금 이력 API(`GET /api/ar/payments`)도 마찬가지로 미사용 |

**어쩌다 그 상태가 되나** — 배분 1건 삭제 로직:

```js
if (remain === 0 && advance_amount === 0) { …입금 헤더까지 삭제… }
else { UPDATE sales_payments SET amount = amount - $1 }   // ← 선수금이 있으면 헤더가 남는다
```

선수금이 붙은 입금건에서 배분을 지우면 **헤더가 선수금만 안은 채 남고, 배분 0건이라 화면에서 영영 사라진다.**
구 동작(배분하지 않은 나머지를 자동으로 전액 선수금 처리 — 같은 날 `bank_deposit_partial_alloc` 에서 고친 그 동작)과 겹치면 정확히 이 그림이 된다. **돈은 원장에 남아 있는데 손댈 방법이 없는 상태**였다.

## ② 해결 — 수금/정산에 「선수금(과입금)」 카드

미배분 입금 인박스 바로 아래, **수금 상세 위**에 새 카드. 선수금이 남은 입금건이 하나도 없으면 카드 자체가 숨겨진다.

**의미 정리(중요)**: `sales_payments.advance_amount` 를 이제 **"아직 인보이스에 배분되지 않고 남은 선수금"** 으로 명확히 정의한다. 배분하면 줄고, 0이 되면 목록에서 사라진다. (기존에도 값의 의미는 같았지만 **감소시키는 경로가 없었다.**)

### 두 가지 처리 경로 (둘 다 디렉터 전용)

**⒜ [인보이스에 배분]** — *진짜 과입금일 때.*
현금은 **이미 계좌에 들어와 있다**(선수금 2030 거래로 기표됨). 그래서 새 현금을 만들지 않고 **선수금을 매출수금으로 옮긴다**:

- 선수금 거래(2030 부채)를 배분액만큼 **축소**(전액 배분이면 소프트 삭제)
- 같은 금액의 **매출수금 거래(4010)** 를 인보이스에 연결해 생성
- → **계좌 잔액 불변**, 인보이스 미수만 줄어든다. 이중계상 없음.

화면: 그 고객의 미수 인보이스를 펼쳐 인보이스별 금액 입력. **[오래된 순 자동 채움]** 버튼으로 오래된 인보이스부터 선수금을 소진시킬 수 있다(수금 실무 기본값). 합계가 선수금을 넘으면 저장 버튼이 잠긴다.

**⒝ [입금 취소]** — *잘못 생긴 건일 때.*
기존 `DELETE /api/ar/payments/:id` 를 화면에 연결했다. 입금건을 통째로 되돌린다:
배분이 함께 있으면 그 인보이스 미수도 복구 · 관련 거래 전부 소프트 취소 · **연결된 입금통지는 미배분 인박스로 복귀**(0191 의 `bdReleaseAmount` 경로) → 처음부터 다시 반제하면 된다.
확인창에 "함께 지워지는 반제 N건 / 선수금 얼마 / 통지가 인박스로 복귀" 를 **미리 다 보여준다.**

### 변경 상세

**백엔드 `refatrix-api/src/routes/financeRoutes.js`** (마이그레이션 없음)

| 엔드포인트 | 내용 |
|---|---|
| `GET /api/ar/advances` (신규) | 선수금 잔여 > 0 인 입금건 목록. `advance_amount`·`allocated`·`alloc_count`·`deposit_id`(어느 통지에서 온 돈인지) 포함. **settlement 열람권** + 팀 범위 적용 |
| `POST /api/ar/advances/:id/apply` (신규) | 선수금을 인보이스에 배분. **디렉터 전용.** 2030 축소 + 4010 생성(잔액 불변). 가드: 선수금 초과(`exceeds_advance`) · 같은 고객 아님(`not_customer_invoice`) · 인보이스 미수 초과(`over_outstanding`) · 빈 배분(`no_allocations`) |
| `POST /api/ar/payments` | 응답 `id` 를 `Number()` 로 변환 — node-pg 가 BIGINT 를 **문자열**로 주던 것을 바로잡음(다른 AR 라우트와 일관) |

**프런트 `refatrix-settlement.html`**
- 「선수금(과입금)」 카드 + 배지(건수) + 잔여 합계 tfoot.
- 행: 입금일 · 고객 · 계좌 · 입금 총액 · 배분됨(건수) · **선수금 잔여** · 메모 · 등록자 · 액션.
- 디렉터가 아니면 액션 자리에 "디렉터만 처리 가능".
- 배분 패널: 고객 미수 인보이스 표(`/api/ar/open-invoices` 재사용) + 금액 입력 + 합계/잔여/초과 경고 + [오래된 순 자동 채움].
- 반제 저장·배분 삭제 후 이 카드를 자동 갱신. 배분 삭제 시 헤더가 남으면 **"선수금이 남아 있으면 위 카드에서 처리하세요"** 안내를 메시지에 덧붙인다(사라진 것처럼 느끼지 않도록).

---

## ③ 배포

앞 두 작업과 **같은 zip · 같은 순서**: 백엔드 push → Railway Success → **`npm run migrate`**(0191) → Pages → 수금/정산 **Ctrl+Shift+R**(nav `v20260831bd`).

검증:
```
curl -s ".../main/refatrix-api/src/routes/financeRoutes.js?nc=$(date +%s)" | grep -c "api/ar/advances"   # 2+
curl -s ".../main/refatrix-settlement.html?nc=$(date +%s)" | grep -c "선수금(과입금)"                     # 1+
```

## ④ 지금 그 건을 어떻게 정리하나 (배포 후)

1. 수금/정산 진입 → 상단에 **「선수금(과입금)」 카드**가 떠 있는지 확인. 문제의 그 건이 목록에 보일 것이다(고객·입금일·잔여로 식별).
2. **그 돈이 원래 어느 인보이스 반제였는지 아는 경우** → **[인보이스에 배분]** → 해당 인보이스에 금액 입력 → **[배분 저장]**. 계좌 잔액은 그대로, 인보이스 미수만 줄어든다. (되돌리기 가장 안전한 경로 — 새 거래를 만들지 않는다.)
3. **처음부터 다시 하고 싶으면** → **[입금 취소]** → 통지가 미배분 인박스로 복귀 → 인박스에서 정상 절차로 재반제.
4. 어느 쪽이든 처리 후 **거래목록**에서 선수금(2030) 잔액이 그만큼 줄었는지 대조.

> 판단 기준: **돈이 실제로 들어온 게 맞으면 [배분]**(원장에 남겨둠), **입금 자체가 잘못 잡힌 것이면 [취소]**.

## ⑤ 테스트 방법 (운영 스모크)

1. 통지를 하나 등록하고 인보이스 하나에 **일부만 배분 + [잔여를 선수금 처리하고 닫기]** → 선수금 카드에 뜨는지.
2. **[인보이스에 배분]** → 다른 미수 인보이스에 배분 → 저장 → ⓐ 인보이스 미수 감소 ⓑ **계좌 잔액 불변** ⓒ 거래목록에서 선수금(2030)이 줄고 매출수금(4010)이 같은 금액 생겼는지.
3. 부분 배분 후 카드에 **남은 선수금**이 정확히 표시되는지.
4. **[오래된 순 자동 채움]** → 오래된 인보이스부터 채워지는지.
5. 선수금보다 크게 입력 → 저장 버튼 잠김 + "선수금 초과" 경고.
6. **[입금 취소]** → 확인창에 반제 건수·선수금·통지 복귀가 안내되는지 → 취소 후 ⓐ 인보이스 미수 복구 ⓑ 통지가 **미배분 인박스에 잔여 전액으로** 복귀 ⓒ 계좌에서 그만큼 빠짐.
7. 영업지원 계정: 목록은 보이되 액션 자리에 "디렉터만 처리 가능".
8. **회귀**: 선수금 없이 정상 반제한 건은 이 카드에 뜨지 않는다.

## ⑥ 검증 (이번 세션)

- `node --check` 백엔드 통과 · `refatrix-settlement.html` 인라인 `<script>` 문법 0 errors.
- **신규 `refatrix-api/test/ar_advance_manage.test.mjs` — 종단 9건 전부 통과**
  ① **버그 재현**: 배분 삭제 → 헤더가 선수금만 안고 남음 → **인보이스 수금내역 0건**(사라짐) 이지만 **선수금 목록에는 잡힌다** · `deposit_id` 로 출처 추적
  ② 전액 배분: 인보이스 완납 · **계좌 입금 합계 불변** · 선수금 거래 소멸 · 4010 1건 생성 · 목록에서 사라지고 수금내역에 다시 등장
  ③ 부분 배분: 잔여 6,600 · **2030 거래가 6,600 으로 축소** · 잔액 불변
  ④ 가드 4종(선수금 초과 · 빈 배분 · 남의 고객 인보이스 · 인보이스 미수 초과)
  ⑤ 권한(배분은 디렉터 전용 403 / 조회는 settlement 권한자 200)
  ⑥ 입금 취소: 선수금·배분 모두 복구 · **입금 총액만큼 계좌에서 빠짐** · **통지가 잔여 전액으로 인박스 복귀**
  ⑦ 선수금 0 인 정상 반제는 목록에 안 뜸(회귀)
  - 같은 DB 에 **3회 연속 재실행** 9/9(정리 멱등).
- **전체 스위트 회귀**: 변경 전(HEAD)·후를 각각 새 DB 에 마이그레이션해 비교 — 실패 목록 동일, **새로 깨진 테스트 0건**.

## ⑦ 핵심 학습 / 원칙

- **모든 상태에는 화면이 있어야 한다.** 데이터가 만들 수 있는 상태(배분 0 + 선수금 > 0)를 화면이 하나도 보여주지 않으면, 사용자에게는 "돈이 사라진" 것이고 복구 수단이 없다. **API 가 있는데 부르는 화면이 없는 것**은 없는 것과 같다.
- **되돌리기는 만든 경로마다 짝을 맞춘다.** 선수금을 *만드는* 경로는 여럿(자동 과입금·통지 잔여 확정·배분 삭제)인데 *푸는* 경로가 0개였다. 상태를 만드는 코드를 쓸 때 푸는 코드를 같이 쓴다.
- **현금은 두 번 만들지 않는다.** 선수금을 인보이스에 배분할 때 4010 만 새로 만들고 2030 을 그대로 두면 계좌 잔액이 부풀어 오른다. 이미 들어온 돈의 **재분류**는 한쪽을 줄이고 한쪽을 늘리는 쌍으로 처리한다.
- **파괴적 동작은 결과를 먼저 보여준다.** [입금 취소] 확인창이 "반제 N건이 함께 지워지고 통지가 인박스로 돌아간다"를 미리 말해주면, 사용자가 되돌릴 수 없는 선택을 눈 감고 하지 않는다.
- node-pg 는 BIGINT 를 **문자열**로 준다. 라우트 응답에 id 를 실을 땐 항상 `Number()` — 이번에 `POST /api/ar/payments` 에서 실제로 물렸다(테스트에서 `===` 비교가 조용히 실패).

## ⑧ 열린 메모 / 다음에 할 일

- **선수금 잔액을 고객 화면에도**: `/api/ar/customers` 는 이미 고객별 `advance` 합계를 주는데 쓰는 화면이 없다. 고객 상세에 "선수금 MX$X" 를 띄우면 영업이 먼저 알아챌 수 있다.
- **배분 되돌리기**: 선수금 → 인보이스 배분을 실수로 한 경우, 지금은 수금내역에서 그 배분을 삭제하면 되지만 **선수금으로 되돌아가지는 않는다**(입금 헤더의 advance_amount 는 그대로 0). 필요하면 `DELETE /api/ar/allocations/:id` 에서 "선수금 배분분이면 advance_amount 로 환원" 로직을 추가해야 한다. — **다음 세션 1순위 후보.**
- 선수금 목록 상한 200건. 쌓이면 고객·기간 필터 필요.
- **`ar_paid_eps`(2026-08-27) 미반영** 경고는 여전히 유효 — 첫 인수인계 문서 상단 참조.

---
*이 문서는 인수인계용 스냅샷입니다. 큰 변경이 생기면 갱신해서 다시 올려주세요.*
