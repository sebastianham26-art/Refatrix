# 검수 개편(build 20260818b) 배포 순서 — 반드시 이 순서대로

"스캔은 기록, 판정은 보고서" 개편 + **이중 스캔 기록 수정**입니다.
스캔 시점 판정·차단이 전부 사라지고, 모든 스캔이 즉시 서버(inbound_scans)에 저장되며,
스캔마다 고유 키가 붙어 전송이 재시도돼도 **정확히 1회만** 기록됩니다(0175).
적치 화면에는 **✎ 수정 기능**(이미 올린 박스의 위치 변경·빼기)이 추가됐습니다.
그리고 **검수 완료 → 적치 전 입고**가 가능합니다: 마감(입고)이 실측 스캔 수량으로
구매에 반영되고, 적치는 마감 후에도 계속 진행됩니다(판매 대응 앞당김).
**백엔드 먼저** 배포해야 합니다.

## ① 백엔드 (Railway)

1. GitHub 저장소에 아래 5개 파일을 덮어쓰기 업로드:
   - `refatrix-api/migrations/0174_inbound_scans.sql` (새 파일)
   - `refatrix-api/migrations/0175_inbound_scan_idempotency.sql` (새 파일)
   - `refatrix-api/migrations/0176_inbound_close_stock.sql` (새 파일 — 마감 즉시 재고 반영)
   - `refatrix-api/migrations/0177_inbound_po_applied.sql` (새 파일 — 구매 재매칭)
   - `refatrix-api/migrations/0178_import_batch_shipment_link.sql` (새 파일 — 수입원가↔선적 연결)
   - `refatrix-api/src/routes/inboundRoutes.js` (교체)
   - `refatrix-api/src/routes/importRoutes.js` (교체 — 수입원가 승인 이중 방지)
   - `refatrix-api/src/routes/purchaseRoutes.js` (교체 — 구매 목록 입고 현황)
2. Railway 가 자동 재배포될 때까지 대기 (Deployments 초록색)
3. Railway 콘솔에서: `npm run migrate`  ← **0174~0178 적용 필수**

## ② 프런트 (GitHub Pages / Cloudflare Pages)

1. 저장소 루트에 덮어쓰기 업로드:
   - `refatrix-inbound.html` (build 20260818b)
   - `refatrix-inbound2.html` (같은 내용 — 캐시 우회용 주소)
   - `refatrix-purchase.html` (build pur-0818a — [입고]·[미입고] 열 추가)
   - `refatrix-import.html` (build imp-0818e — 인보이스 단가 자동 읽기 + 차이 비교 + 목록 스크롤)
   - `refatrix-importcost.html` (build ic-0818b — 분배 대상에 없는 선적 안내)
2. 배포 후 접속. 캐시가 의심되면 `.../refatrix-inbound2.html` 주소 사용
3. F12 콘솔에서 `[refatrix-inbound] build 20260818b` 확인

## 이중 스캔이 이미 기록된 팔렛 정리

이미 2회로 잡힌 스캔은 집계표의 [−] 버튼으로 한 건 빼면 됩니다(서버에서도 취소됨).
여러 팔렛이 엉켰으면 [🧹 검수 리셋] 후 다시 스캔하세요.

## ③ 26B2C 선적 정리 (디렉터 계정, 딱 한 번)

1. 선적 열기 → 상단 **[🧹 검수 리셋]** → 확인
   (잘못 저장된 검수·적치·스캔이 초기화됩니다. 기록은 감사용으로 보존)
2. **[🔀 라인 재분할]** → 원본 패킹리스트 선택
   (CARTON UNIT × QTY PER C/T 기준으로 카톤 수가 교정됩니다)
3. 검수 탭에서 새 방식으로 작업 시작

## ④ 새 검수 절차 (작업자)

1. **팔렛 선택** — 칩을 눌러 지금 작업할 팔렛을 고른다 (시스템 추측 없음)
2. **전부 스캔** — 어떤 경우에도 막지 않는다. 스캔마다 존이 크게 표시되고
   즉시 서버에 저장된다 (배너에 ● 서버 저장됨 / 전송 대기 표시)
3. **[📋 대조]** — 서버가 패킹리스트와 비교한 예상/스캔/차이 보고서
4. **[✔ 입고 확정]** — 실제 스캔 수 그대로 저장. 차이가 있어도 확정 가능,
   부족분은 나중에 더 스캔하고 재확정하면 된다

오스캔은 집계표의 [−] 버튼으로 빼면 서버에서도 최근 1건이 취소됩니다.
네트워크가 끊겨도 스캔은 큐에 남아 자동 재전송됩니다(새로고침에도 보존).

## 테스트 (모두 통과 확인됨)

- tests/scan_label.test.js — 새 검수 흐름 + 이중 기록 방지 + 오류표시/복구 48/48
- tests/put_edit.test.js — 적치 수정(빼기·위치 변경) 11/11
- tests/close_ready.test.js — 검수 완료 즉시 입고·실측 마감 12/12
- tests/scan_hyphen.test.js — 자판 보정 회귀 23/23
- tests/zone_ui.test.js — 존 표시 46/46
- refatrix-api/inbound_scans.test.js — 배정·tally·취소·리셋·멱등·적치수정·마감집계 32/32 (실 PostgreSQL)
- 기존: inbound_lines 23/23 · zones 27/27 · parser_cu 11/11

## 적치 수정 사용법 (build p)

적치 작업 화면의 SKU 줄마다 ✎ 버튼이 있습니다. 누르면:
- **[−1 빼기]** — 랙에서 내린 박스만큼 누르면 적치 수량이 줄어듭니다(서버 반영, 0 밑으로 안 내려감)
- **[+1 추가]** — 스캔 없이 수동으로 1개 추가
- **[위치 적용]** — 새 랙을 스캔/입력하면 이 선적의 적치 위치가 바뀝니다.
  "제품 기본 위치도 변경" 체크(기본 켜짐) 시 제품 마스터 위치도 함께 갱신되고 감사로그에 남습니다.
변경은 2.5초 후 자동 저장되며 [✔ 적치 저장]으로 즉시 저장할 수도 있습니다.

## 검수 후 바로 입고 (build r)

적치를 기다릴 필요 없이, **모든 팔렛 검수가 끝나면 [마감] 탭에서 바로 입고**할 수 있습니다.
- 마감 수량은 이제 패킹리스트 예상이 아니라 **실측 스캔 수량**(부족/초과 그대로)으로 구매 received_qty 에 반영됩니다.
- 마감 후에도 적치 탭은 그대로 사용 가능합니다 — 판매 대응과 적치를 병행하세요.
- 미검수 팔렛이 있으면 그 팔렛은 이번 입고에서 제외된다는 경고가 표시됩니다.
- 마지막 팔렛 검수를 확정하면 "지금 바로 입고 가능" 안내가 자동으로 뜹니다.

## 2026-08-18a 변경 사항

**① "검수중 팔렛이 다시 생겨나는" 버그 수정** — 적치가 진행되면 팔렛 상태가 내부적으로
'checking'이 되는데, 화면이 이를 "검수중"으로 표시하고 마감 집계에서도 빼버렸습니다.
이제 검수 확정(checked_at) 기준으로 판정: 화면에는 "검수완료·적치중"으로 표시되고 마감에도 포함됩니다.

**② 마감 즉시 실재고 반영** — 마감하면 실측 스캔 수량이 products 재고에 바로 더해져 판매 대응 가능.
수입원가 배치를 나중에 승인해도 수량이 이중으로 늘지 않습니다(선반영 풀 0176으로 자동 차감,
원가·평균원가·재고원장은 기존대로 승인 시점 반영).

**③ 재마감(추가 입고 반영)** — 마감 후 새로 검수한 팔렛이 생기면 마감 탭의
[➕ 추가 입고 반영] 버튼으로 이어서 반영. 이미 반영된 팔렛은 이중 반영되지 않습니다(received_at).
구매 미매칭·미등록 SKU·실측 0 항목은 마감 탭에 경고로 표시됩니다.

**④ 적치 팔렛 바코드 드릴다운** — 적치 목록/작업 화면 스캔칸에 팔렛 라벨(예: 26B2C-14,
구분자 없어도 됨, 번호 단독도 유일하면 인식)을 스캔하면 그 팔렛으로 바로 들어갑니다.
카톤 라벨을 스캔하면 기존처럼 해당 카톤이 실린 팔렛이 열립니다. 검수 전 팔렛은 경고.

**오늘 발생한 26B2C 건 복구**: 배포 후 마감 탭에 [➕ 추가 입고 반영] 버튼이 보이면 한 번 눌러주세요 —
빠졌던 팔렛(적치중이던 것 포함)의 실측 수량이 재고와 구매에 반영됩니다.
"주문 수량 0" SKU는 반영 후에도 경고 목록에 남으면 ORDER NO ↔ 구매 발주번호 불일치이니 알려주세요.

## 2026-08-18b — "구매 발주에서 못 찾음" 대량 경고 해결

마감은 성공했고 **재고는 정상 반영**되었습니다. 경고는 구매 발주 연동만 실패한 것으로,
원인은 발주번호 표기 차이(공백·기호) 또는 발주 업로드 당시 제품 미매칭(product NULL)입니다.

- 구매 매칭을 4단계로 확장: 정확 일치 → 표기 차이 무시 → 발주 라인 제품코드로 발견(+백필)
  → 발주번호 달라도 그 제품의 열린 발주에서 소진.
- **[🔧 구매 재매칭]** 버튼(마감 탭, 디렉터): 이미 마감된 팔렛의 구매 연동을 "부족분만" 다시 반영.
  반영량이 기록(0177)되므로 몇 번을 눌러도 이중 반영되지 않습니다.

### 이번 건 처리 순서
1. 백엔드(0177 + inboundRoutes.js) 업로드 → 재배포 → `npm run migrate`
2. 프런트 2개 파일 업로드 (build 20260818b)
3. 해당 선적 → 마감 탭 → **[🔧 구매 재매칭]** 클릭 → 반영 결과 확인
4. 그래도 "미해결"로 남는 건 = 구매 모듈에 그 발주 자체가 없는 것 → 발주 업로드 후 다시 재매칭
5. 미등록 SKU 3건(CE0088L·CE0910L·CL0211L)은 제품 마스터 등록이 먼저 필요합니다(등록 후 알려주시면 반영 경로 추가)

## 2026-08-18c — "마감했더니 구매내역이 사라짐" 설명 + 입고 현황 표시

**데이터는 삭제되지 않았습니다.** 마감 = 입고 완료 처리이므로,
"입고 예정"과 "미입고(backorder)" 목록은 **아직 안 들어온 것만** 보여주는 화면이라
마감된 선적·채워진 발주가 거기서 빠지는 것은 정상 동작입니다.

입고된 발주를 확인할 수 있도록 구매 화면을 보강했습니다(build pur-0818a):
- 구매내역 조회 목록에 **[입고]·[미입고] 열** 추가 — 전량 입고되면 "✔ 입고완료" 표시
- 발주를 클릭한 상세에 **[입고수량] 열** 추가(라인별, 전량 입고 시 녹색)
- 입고 예정 목록이 비면 "마감된 선적은 여기서 빠집니다 → 구매내역 조회에서 확인" 안내 표시

## 2026-08-18d — 수입원가 절차 (디렉터 질문 답변 반영)

**Q. 마감한 재고는 수입원가 등록해야 가용재고가 되나?**
→ 아니요. 20260818a 부터 **마감 즉시 실측 수량이 가용재고(stock)에 반영**됩니다.
수입원가 등록·승인은 **원가(평균원가·재고원장)만** 반영하며 수량은 이중 반영되지 않습니다.

**Q. 수입원가 등록 때 분배 대상(입고 인보이스)을 선택하게.**
→ 수입 입고(원가) 화면 상단에 **"📦 입고(마감) 선적에서 불러오기"** 추가(imp-0818a):
- 마감된 선적이 인보이스 번호·SKU 수·실측 수량과 함께 드롭다운에 나타남
- 선택 → [불러오기] → 실측 수량 + 구매 발주 단가가 라인에 자동 채워짐(단가 없으면 표시 후 직접 입력)
- 부대비용(운임 등) 입력 → 작성+미리보기 → 디렉터 승인 = 그 인보이스 수입에 원가 배부
- 배치가 선적에 연결(0178)되어, 같은 선적으로 이중 등록하면 차단됨("이미 원가 배치 등록됨")
- 이미 등록된 선적은 목록에 "원가 등록됨/승인됨"으로 표시

## 2026-08-18e — "분배 대상에 인보이스가 안 보임" (수입 부대비용 분배 화면)

분배 대상 목록에는 **승인된 수입배치**만 나타납니다. 마감(입고)만 한 선적은 배치가 아직 없어서 안 보입니다.
절차: ① 수입 입고 화면 → [📦 입고 선적에서 불러오기]로 해당 인보이스 선택 → 배치 작성 ② 디렉터 승인
③ 그 다음 수입 부대비용 분배 화면의 분배 대상에 인보이스 번호(batch_no)로 나타남 → 비용 배분.
ic-0818a 부터는 분배 대상 위에 "입고됐지만 아직 분배 대상에 없는 선적"이 노란 박스로 표시되고,
배치 미등록/승인 대기 중 어떤 단계가 남았는지 안내합니다.

## 2026-08-18f — "지금 입고하는 선적이 드롭다운에 안 보임"

원인 두 가지를 화면에서 바로 알 수 있게 했습니다(imp-0818b, ic-0818b + importRoutes.js 재배포):
1. **아직 마감 전인 선적** — 이제 드롭다운에 "입고 진행 중(예상 n개) — 창고 마감 후 선택 가능"으로
   표시됩니다(회색, 선택 불가). 창고에서 검수·마감을 마치면 실측 수량과 함께 선택 가능해집니다.
2. **백엔드 미배포** — importRoutes.js/0178 이 서버에 없으면 드롭다운에
   "서버에 새 API 없음 — 백엔드 배포와 npm run migrate(0178) 확인"이 표시됩니다.
비용 분배 화면의 노란 안내 박스도 "입고 진행 중" 단계를 함께 보여줍니다.

## 2026-08-18g — 수입단가 0으로 표기되는 SKU

원인: [불러오기]의 단가는 구매 발주 라인에서 오는데, 발주 업로드 당시 제품 미매칭(product NULL)이었던
라인은 단가를 못 찾아 0으로 비었습니다(불러올 때 "단가 미확인 n건" 경고 항목).

수정(imp-0818c + importRoutes.js):
- 단가 조회가 미매칭 발주 라인도 **제품코드로** 찾아옵니다(GV-0828 ↔ GV0828 표기 차이 무시)
- 단가 0인 라인이 있으면 작성 전에 확인창으로 경고합니다(0원 승인 = 평균원가 왜곡 방지)

이미 0단가로 작성한 배치가 있다면: 승인하지 말고 **반려** → 새 파일 배포 후 다시 [불러오기] → 작성.
(반려된 배치는 이중 등록 검사에서 제외되므로 같은 선적으로 재등록 가능)

## 2026-08-18h — 단가 필수 입력 절차 (imp-0818d)

수입단가가 비어 있으면 **다음 단계(작성+미리보기)로 넘어갈 수 없습니다**:
- [불러오기] 직후 단가가 비어 있는 칸이 **붉게 표시**되고 미입력 건수가 안내됩니다
- [① 작성+미리보기]를 눌러도 미입력이 남아 있으면 차단되고, 첫 미입력 칸으로 자동 이동합니다
- 단가를 입력하면 붉은 표시가 즉시 풀리고, 전부 입력해야 진행됩니다
- 발주에 단가가 있는 품목은: 창고 마감 탭 [🔧 구매 재매칭] → 다시 [불러오기] 하면 자동으로 채워집니다
- 테스트: tests/import_price.test.js 7/7 (차단·표시·해제·진행)

## 2026-08-18i — 인보이스 단가 자동 읽기 + 목록 스크롤 (imp-0818e)

**인보이스로 단가 입력**: [📄 인보이스 단가 읽기] 버튼 → 공급사 인보이스(엑셀) 업로드 →
코드·단가 열을 자동으로 찾아(CTR NO/UNIT PRICE 류, 제목행이 있어도 됨) 라인에 단가를 채웁니다.
- **등록된 구매단가(발주가)와 다르면**: 해당 칸 노란 표시 + "발주단가 vs 인보이스 vs 차이(%)" 비교 표
- 수량이 인보이스와 다른 라인, 인보이스에 없는 라인(직접 입력 필요), 라인에 없는 인보이스 코드도 각각 안내
- 인보이스에 없어 여전히 빈 단가는 기존 필수 입력 게이트가 그대로 차단

**목록 보기 개선**: 입고 라인 목록이 자체 스크롤(최대 55% 화면)로 바뀌어 수백 라인이어도
페이지가 끝없이 길어지지 않습니다. 코드 필터 입력창 + [단가 미입력만 보기] 체크 + 라인 카운트 추가.
미리보기 표도 스크롤 적용.

테스트: tests/import_price.test.js 17/17 (필수 차단 + 인보이스 적용·차이표·표기차 매칭·필터)
