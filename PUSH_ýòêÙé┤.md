# 카탈로그 조회 API — 덮어쓰기용 묶음 (스크립트 필요 없음)

지금 레포(`3d25b96 카달록 고객이 가져가기`)의 **실제 파일을 받아서 고친 것**이다.
압축을 레포 최상위에 그대로 풀고 커밋·푸시하면 된다. 스크립트를 돌릴 필요가 없다.

## 든 것 — 54개 파일

| 파일 | 무엇이 바뀌었나 |
|---|---|
| `refatrix-api/src/server.js` | 카탈로그 라우트 등록 2줄 추가 |
| `refatrix-nav.js` | 메뉴 등록 · 디렉터 권한 · 관리 그룹 3군데 추가 |
| `*.html` (52개) | 메뉴 캐시 토큰 `20260917cotiz` → `20260917catalog` |

**견적요청 수신은 그대로 살아 있다.** `crmQuoteRoutes` 등록과 견적 팝업(`startCrmQuoteAlert`)이
그대로 있는 것을 확인했고, 두 파일 모두 문법 검사를 통과했다.

새 파일들(`catalogApiRoutes.js`·`catalogPull.js`·`0221_catalog_pull_api.sql`·`refatrix-catalog-api.html`)은
**이미 레포에 올라가 있으므로** 이 묶음에 넣지 않았다.

## 순서

1. 압축을 레포 최상위에 풀기 (덮어쓰기)
2. 커밋 · 푸시 → Railway 자동 배포
3. Railway 에서 **`npm run migrate`** (0221 적용)
4. Ctrl+Shift+R → 포털 관리 그룹에 **「카탈로그 조회 API」**

## 그다음 화면에서

「+ 고객사 추가」 → 연결 고객 선택(할인율 확인) → 접속창 토요일 5~9 →
**사진 주소 규칙**에 아래 값을 넣는다(비우면 계약서와 다른 주소가 나간다):

```
https://pub-d34920cb200c42ce91c5cbda135f16d6.r2.dev/products/{code}/{code}_1.webp
```

→ 「미리보기」로 가격·사진 확인 → 테스트 키 발급 → 검수 후 운영 키.
