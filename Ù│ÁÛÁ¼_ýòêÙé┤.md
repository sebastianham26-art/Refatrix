# 카탈로그 조회 API 메뉴 복구 — 2026-09-18

## 무슨 일이 있었나

파일이 지워진 게 아니다. **메뉴 등록 줄만 다른 커밋에 덮여 사라졌다.**

| 커밋 | nav.js 의 catalogApi | server.js 의 catalogApiRoutes |
|---|---|---|
| `77f293e` 카달록 api 로그인 화면 | 3군데 있음 | 2줄 있음 |
| `1f0d4ff` CRM에서 견적받을 때 팝업 | **0 — 사라짐** | **0 — 사라짐** |
| 이후 현재까지 | 0 | 0 |

`1f0d4ff` 이 `refatrix-nav.js` 와 `src/server.js` 를 **통째로 덮어썼다**(server.js 에서 정확히 2줄이
빠졌다). 그 두 파일은 여러 작업이 함께 고치는 파일이라, 한 작업의 묶음으로 통째로 덮으면
다른 작업이 조용히 사라진다. 이번엔 카탈로그가 당했고, 반대 방향이었다면 견적 팝업이 사라졌을 것이다.

**나머지는 전부 살아 있다** — 카탈로그 화면·라우트·재고 구간·PRO 제외·로그인 수정 모두 그대로다.
복구할 것은 **5줄뿐**이다.

## 이 묶음

현재 `origin/main`(`b5ee01c`)의 실제 파일에 그 5줄을 다시 넣은 것이다. 압축을 레포 최상위에
그대로 풀고 커밋·푸시하면 된다.

| 파일 | 내용 |
|---|---|
| `refatrix-api/src/server.js` | 라우트 등록 2줄 |
| `refatrix-nav.js` | 메뉴·권한·관리그룹 3군데 |
| `*.html` (53개) | 메뉴 캐시 토큰 → `20260918catalog` |

**견적요청 수신은 그대로 살아 있다** — `crmQuoteRoutes` 등록과 견적 팝업이 남아 있는 것을
확인했고, 두 파일 모두 문법 검사를 통과했다.

## 순서

1. 압축을 레포 최상위에 풀기(덮어쓰기)
2. 커밋 · 푸시 → Railway 배포
3. Ctrl+Shift+R → 포털 관리 그룹에 **「카탈로그 조회 API」**

마이그레이션은 이미 올라가 있으므로(`0221`·`0222`·`0223`) 아직 `npm run migrate` 를 안 했다면
이번에 한 번 돌린다.

## 다시 안 당하려면

`refatrix-nav.js` 와 `refatrix-api/src/server.js` 는 **통째로 덮지 않는다.** 새 기능 묶음을 받으면
그 두 파일은 필요한 줄만 넣는다. 덮어쓴 뒤에는 커밋 전에 한 번만 확인하면 된다:

```bash
grep -c catalogApi refatrix-nav.js                    # 3 이어야 한다
grep -c catalogApiRoutes refatrix-api/src/server.js   # 2 이어야 한다
grep -c crmQuoteRoutes  refatrix-api/src/server.js    # 2 이어야 한다
```
