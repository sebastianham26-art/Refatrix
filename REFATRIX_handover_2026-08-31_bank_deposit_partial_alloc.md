# REFATRIX 인수인계 — 입금통지 「부분 배분」: 인보이스 1건만 먼저 반제해도 잔여가 남는다

- 작업일: 2026-08-31
- 마이그레이션: **0191** (신규 · `0191_bank_deposit_partial_alloc.sql`) → `npm run migrate` **필요**
- 변경 파일: 백엔드 1 · 프런트 2 · 마이그레이션 1 · 테스트 1(신규)
- **nav 캐시 마커**: `v20260831bd` (직전 `v20260824ph`) — `refatrix-nav.js` 자체는 무변경, 두 화면의 토큰만 상향
- 산출물 zip: `refatrix_20260831_bd_partial.zip` (레포 루트에서 풀면 제자리)
- 검증: 실 PostgreSQL 16 · 0001~0191 전체 적용 **종단 13건 전부 통과**(3회 연속 재실행 포함) · 전체 스위트 회귀 **0건**

> ⚠️ **먼저 확인**: 2026-08-27 `ar_paid_eps` 패키지(`refatrix_20260827_ar_paid_eps.zip`)가 아직 레포(main)에 반영돼 있지 않습니다.
> `src/ar.js`·`financeRoutes.js`·`refatrix-settlement.html` 어디에도 `AR_PAID_EPS` 가 없습니다.
> 이번 zip은 **현재 main 기준**으로 만들었고 같은 파일 2개(`financeRoutes.js`, `refatrix-settlement.html`)를 건드립니다.
> 그 패키지도 올릴 계획이면 **ar_paid_eps 를 먼저 적용·커밋한 뒤** 이 zip 을 받아 다시 만들어야 합니다.
> 순서를 바꾸면 둘 중 하나가 통째로 묻힙니다.

---

## ① 증상 / 원인

**증상(디렉터 보고)**: 고객이 인보이스 여러 건을 **한 번에 송금**하면 입금통지는 1건인데 반제할 인보이스는 여러 건이다.
이때 **인보이스 1건을 먼저 반제하면, 돈이 남아 있어도 입금통지 항목에 「잔여」로 나오지 않는다.**

**원인 — 통지에 "부분 배분" 개념이 없었다**

`POST /api/ar/payments` 는 반제 1회에 무조건 다음을 했다:

```js
const amount  = r2(Number(dep.amount));   // 반제 금액 = 통지 전액(고정)
const advance = r2(amount - sumAlloc);    // 배분하고 남은 돈 = 전부 선수금
… UPDATE bank_deposits_pending SET status='allocated' …   // 통지를 즉시 닫음
```

즉 **한 통지 : 한 반제** 구조였다. 결과:

- 인보이스 1건(11,600)만 배분하고 저장하면 → 남은 58,000이 **그 자리에서 선수금(과입금) 거래**로 빠지고,
- 통지는 `allocated` 로 **닫혀 인박스에서 사라진다** → 나머지 인보이스를 이어서 반제할 방법이 없음.

화면의 배분 카트(`bdCart`)는 "여러 인보이스를 **한 번에** 담아 한 번에 저장"만 지원했으므로,
- 나머지 인보이스가 아직 발행 전이거나
- 어떻게 쪼갤지 아직 모르거나
- 그냥 1건만 담고 저장을 눌러버리면

그대로 이 함정에 빠졌다. (2026-06-24 미배분입금 인수인계 ⑧의 "다중 인보이스 입금 자동 분할 미구현" 메모가 바로 이 건이다.)

## ② 해결 — 통지에 `allocated_amount`(소진액)를 두고 잔여를 남긴다

**잔여 = 통지금액 − 소진액. 잔여가 남아 있으면 통지는 `pending` 유지 → 인박스에 「부분배분 · 잔여 MX$X」로 계속 보인다.**

디렉터 확정(2026-08-31):
1. 배분하고 남은 금액은 **통지에 잔여로 남긴다**(자동 선수금 처리 안 함). 선수금 확정은 **별도 버튼**으로 명시적으로.
2. 잔여가 남은 통지는 **인박스에만 표시**하고 45초 폴링 팝업은 다시 띄우지 않는다(이미 읽은 통지이므로).

### 마이그레이션 0191

| 대상 | 내용 |
|---|---|
| `bank_deposits_pending.allocated_amount` | `NUMERIC(15,2) NOT NULL DEFAULT 0` — 이 통지에서 지금까지 소진된 금액(배분 + 확정된 선수금) |
| `bank_deposit_payments` (신규) | `(deposit_id, payment_id, amount, created_by, created_at)` — **한 통지 : 여러 반제** 링크. `payment_id` 는 `ON DELETE CASCADE` |
| 백필 | 기존 `status='allocated'` 통지는 `allocated_amount = amount` 로 채우고 링크행 1건 생성 (기존 동작이 항상 전액 소진이었으므로 의미가 정확히 일치) |

`bank_deposits_pending.payment_id` 는 **"마지막 반제" 포인터**로만 남긴다(하위호환·FK 유지).

### 백엔드 `refatrix-api/src/routes/financeRoutes.js`

| 위치 | 변경 |
|---|---|
| 상단 | `export const BD_REMAIN_EPS = 0.5` — 잔여가 이 값 미만이면 "잔여 없음"으로 보고 닫는다(화면이 정수 페소로 반올림하므로 = 화면상 0). 인보이스 완납 판정의 `AR_PAID_EPS` 와 같은 취지 |
| 상단 | `bdReleaseAmount(c, paymentId, amount, removeLink)` 신설 — 반제/배분 삭제 시 통지 소진액을 그만큼 되돌리고 `payment_id` FK 도 정리. 0191 이전 레거시 반제(링크행 없음)는 `payment_id` 로 찾아 통째로 복귀 |
| `POST /api/ar/payments` | 상한이 **통지 전액 → 통지 잔여**. `amount = 배분합 + (닫을 때만) 잔여 선수금`. `close_deposit` 미지정이면 잔여를 남기고 `status='pending'` 유지, 다 소진했거나 `close_deposit=true` 면 `allocated`. 링크행 INSERT + `allocated_amount` 누적. 응답에 `deposit_closed`·`deposit_used`·`deposit_remaining` 추가 |
| `GET /api/bank-deposits` | `allocated_amount`·`remaining`·`partial` + `alloc_customer_id/name`(이미 배분된 고객 — 이어서 반제할 때 검색 자동 채움용) |
| `GET /api/bank-deposits/unread` | `AND COALESCE(allocated_amount,0) <= 0.001` — 부분배분 통지는 팝업 재발화 안 함 |
| `void` · `PATCH` · `DELETE` · `book-income` | 부분배분(소진액 > 0) 통지는 이미 실거래가 있으므로 **409 `has_allocation`** (금액/계좌 수정, 수입전환, 취소·삭제 전부 차단). 반제를 먼저 되돌려야 한다 |
| `DELETE /api/ar/payments/:id` · `DELETE /api/ar/allocations/:id` | 통지 복귀 로직을 `bdReleaseAmount` 로 교체 — **소진한 만큼만** 잔여 복구(전량 복구가 아니라). 배분 1건만 지우는 경로도 이제 통지 잔여를 되돌린다 |

**회계 원칙은 그대로**: 통지 등록은 `transactions` 미생성, 배분한 금액만큼만 그때그때 거래 1건. 이중계상은 여전히 구조적으로 불가.
다만 **부분배분 중에는 계좌 잔액이 아직 통지 전액을 반영하지 않는다**(미배분 잔여는 통장에는 들어왔지만 원장에는 안 들어온 상태). 인박스에 잔여로 계속 보이는 것이 그 사실을 드러내는 장치다.

### 프런트

**`refatrix-settlement.html`** (영업지원 수금/정산)
- 인박스 표에 **배분됨 / 잔여** 열 추가. 부분배분 행은 파란 배경 + 「부분배분」 칩(새 통지의 노란 배경과 구분).
- 버튼: 부분배분 건은 **[잔여로 이어서 반제]** + **[잔여 선수금 처리]**(잔여를 선수금으로 확정하고 닫기, 확인창 있음).
- `bdUsed()` / `bdRemain()` 헬퍼 — 서버가 `remaining` 을 안 주는(백엔드 미배포) 상황에서도 `amount` 로 폴백해 화면이 깨지지 않음.
- 배분 카트: "선수금(과입금)" 표시를 **"저장 후 통지 잔여"** 로 교체. 버튼이 상황에 따라 둘로 갈린다.
  - 잔여가 남으면: **[저장 · 잔여 MX$X 남기기]** (기본) + **[저장 · 잔여를 선수금 처리하고 닫기]**
  - 딱 맞으면: **[저장 · 통지 닫기]** 하나
- 저장 후 잔여가 남으면 **같은 통지를 선택 상태로 유지**해 바로 다음 인보이스에 이어서 배분할 수 있다.
- 배분 상한·초과 경고 문구가 전부 "통지 금액" → **"통지 잔여"** 기준으로 변경.

**`refatrix-finance.html`** (재무 거래등록 탭 · 미배분 입금 목록)
- **잔여** 열 + 「부분배분」 칩. 부분배분 건은 수입전환·수정·삭제 버튼을 감추고 "영업지원이 일부 반제 중 — 수정·삭제 불가" 로 표시.
- `bdErrText()` 신설 — `has_allocation` 을 "영업지원이 이 입금으로 이미 일부 반제했습니다(배분 MX$X). 수금/정산에서 해당 반제를 먼저 되돌려야 합니다." 로 풀어서 보여준다.

---

## ③ 배포 순서 (백엔드 먼저 · 마이그레이션 있음)

1. zip 압축 해제 → 로컬 repo에 **경로 그대로 덮어쓰기**
   - `refatrix-api/migrations/0191_bank_deposit_partial_alloc.sql` (신규)
   - `refatrix-api/src/routes/financeRoutes.js`
   - `refatrix-api/test/bank_deposit_partial_alloc.test.mjs` (신규)
   - `refatrix-settlement.html` · `refatrix-finance.html`
2. GitHub Desktop **Commit → Push**.
3. **Railway** 자동 재배포 → Deployments **Success** 확인.
4. Railway APP 콘솔에서 **`npm run migrate`** 실행 (0191 적용). ← **필수**
5. GitHub Pages 1~2분 → 수금/정산·재무 화면에서 **Ctrl+Shift+R**. nav 마커 `[refatrix-nav] … v20260831bd`.

> 순서 주의: **마이그레이션 전에 화면만 올리면 안 된다.** `allocated_amount` 컬럼이 없으면 `POST /api/ar/payments` 가 500 이다.
> 반대로 백엔드+마이그레이션만 먼저 올리는 건 안전하다(구 화면은 잔여를 안 보여줄 뿐, 부분 배분은 정상 동작).

검증(운영 반영 확인):
```
curl -s ".../main/refatrix-api/src/routes/financeRoutes.js?nc=$(date +%s)" | grep -c "BD_REMAIN_EPS"        # 1+
curl -s -o /dev/null -w "%{http_code}\n" ".../main/refatrix-api/migrations/0191_bank_deposit_partial_alloc.sql"  # 200
curl -s ".../main/refatrix-settlement.html?nc=$(date +%s)" | grep -c "잔여로 이어서 반제"                  # 1+
```

---

## ④ 테스트 방법 (운영 스모크 — 원래 버그 시나리오 그대로)

1. **(재무 로그인)** 재무 > 거래 등록 → 은행 매출입금 등록: 한 고객의 인보이스 3건 합계(예: 69,600)로 통지 1건 등록.
2. **(영업지원 로그인)** 수금/정산 인박스 → `[이 입금으로 반제]` → **인보이스 1건(11,600)만** 담고 **[저장 · 잔여 MX$58,000 남기기]**.
3. ✅ **인박스에 그 통지가 「부분배분 · 잔여 58,000」 으로 남아 있는지** ← 이게 이번 수정의 핵심.
4. 그 행의 `[잔여로 이어서 반제]` → 두 번째 인보이스 배분 → 저장 → 잔여 34,800 으로 줄어드는지.
5. 세 번째 인보이스로 전액 소진 → **통지가 자동으로 닫히고 인박스에서 사라지는지** · 선수금 0.
6. 거래목록: 이 계좌 입금 거래 합계 = **69,600 정확히 1회분**(이중계상 없음).
7. **잔여 선수금 경로**: 다른 통지로 일부만 반제 → 인박스에서 `[잔여 선수금 처리]` → 확인 → 잔여가 선수금 거래 1건으로 확정되고 통지 닫힘.
8. **가드**: 부분배분 중인 통지를 재무 화면에서 수정·삭제·수입전환 시도 → "영업지원이 이미 일부 반제했습니다" 로 막히는지.
9. **되돌리기**: 디렉터가 그 반제를 취소 → 인박스 잔여가 그만큼 **되돌아오고** 인보이스 미수도 복구되는지.
10. **회귀**: 예전처럼 여러 인보이스를 한 번에 담아 저장 → 통지가 바로 닫히는지(동작 동일).

---

## ⑤ 검증 (이번 세션)

- `node --check` — `financeRoutes.js` 통과. 변경 HTML 2개 인라인 `<script>` 4블록 `new Function` 문법 검증 — 0 errors.
- 마이그레이션 0001~**0191** 실 PostgreSQL 16 에 전체 적용 성공.
- **신규 `refatrix-api/test/bank_deposit_partial_alloc.test.mjs` — 실 PG + 실 Fastify 라우트 종단 13건, 전부 통과**
  ① 버그 재현: 1건만 반제해도 통지가 안 닫히고 잔여 58,000 이 인박스에 남는다 / 선수금 0
  ② 잔여로 이어서 반제 → 34,800 / ③ 전액 소진 → 자동 닫힘 · 입금거래 합 = 통지액(이중계상 0) · 링크 3건
  ④ 소진된 통지 재반제 거부 / ⑤ 초과 판정 기준이 **잔여**(총액 아님) / ⑥ `close_deposit` 잔여 선수금
  ⑦ 센타보 먼지 0.32 자동 선수금 + 닫힘 / ⑧ 부분배분 통지 void·patch·delete·book-income 전부 409 `has_allocation`
  ⑨ 반제 취소 → 잔여·인보이스 미수 복구, 그 후 취소 가능 / ⑩ 부분배분은 unread(팝업)에서 제외
  ⑪ 회귀: 한 번에 여러 인보이스 배분(기존 카트) 동작 동일
  - **3회 연속 재실행 13/13 통과**(테스트 정리 멱등성 확인).
- **전체 스위트 회귀**: 변경 전(HEAD)·후를 **각각 새 DB**에 마이그레이션해 비교 — 실패 목록 동일, **새로 깨진 테스트 0건**.
  (참고: 이 레포의 전체 스위트는 일부 테스트가 `users` 테이블을 파괴적으로 재정의하므로 **한 DB 를 공유해 전체 실행하면 뒤 테스트가 무더기로 실패**한다. 개별 파일 단위 실행이 정확하다.)

---

## ⑥ 핵심 학습 / 원칙

- **"1:1" 이라 가정한 링크는 언젠가 1:N 이 된다.** 통지 1건 = 반제 1건 가정이 이 버그의 본질이었다. 소진액(`allocated_amount`) + 링크 테이블로 바꾸니 자연스럽게 풀렸다.
- **남은 돈을 조용히 다른 계정으로 옮기지 말 것.** 기존 코드는 배분하지 않은 돈을 말없이 선수금으로 처리했다. 사용자가 의도하지 않은 회계 처리는 "돈이 사라진" 것처럼 보인다. 잔여는 **잔여로 남기고**, 선수금 확정은 사람이 명시적으로 누르게.
- **되돌리기는 "전량 복구"가 아니라 "그만큼 복구"** — 부분 배분을 도입하면 취소 경로도 같이 부분화해야 한다(`bdReleaseAmount`). 여기를 빠뜨리면 잔여가 부풀어 이중 반제가 가능해진다.
- **부분 상태에는 잠금을 건다.** 실거래가 이미 붙은 통지의 금액·계좌를 나중에 고칠 수 있으면 원장이 어긋난다 → `has_allocation` 409.
- 임계값은 화면 반올림 기준(0.5)과 맞춘다 — `AR_PAID_EPS` 때 배운 것과 같다. 안 맞추면 "화면엔 잔여 0인데 통지가 안 닫히는" 상태가 생긴다.

## ⑦ 열린 메모 / 다음에 할 일

- **`ar_paid_eps`(2026-08-27) 미반영** — 위 경고 참조. 이 세션 코드에는 `AR_PAID_EPS` 가 들어있지 않다.
- **부분배분 중 계좌 잔액 과소 표시**: 미배분 잔여는 통장에는 있지만 원장에는 없다. 부분배분 잔여 합계를 재무 화면에 "미기표 입금" 카드로 보여주는 안을 검토할 만함.
- **자동 매칭 제안**(금액·날짜로 후보 인보이스 추천)은 여전히 미구현. 부분배분이 생긴 지금 "잔여에 딱 맞는 인보이스" 추천이 특히 유용해졌다.
- 통지 잔여를 **다른 고객** 인보이스에 배분하는 것은 여전히 금지(한 통지 = 한 고객). 실제로 필요한 사례가 나오면 재검토.
- `bank_deposit_payments.amount` 는 배분 삭제 시 차감만 하고 0 이 되어도 행을 남긴다(이력 보존). 정리가 필요하면 별도 배치.

---
*이 문서는 인수인계용 스냅샷입니다. 큰 변경이 생기면 갱신해서 다시 올려주세요.*
