# REFATRIX 인수인계 — 「유령 부분배분」: 반제를 지웠는데 통지 소진액이 남는 문제

- 작업일: **2026-09-02** (같은 날 `ar_paid_eps` 작업에 **이어서** — 같은 zip 으로 배포)
- 마이그레이션: **0194** (신규 · `0194_bank_deposit_alloc_recalc.sql`) → **`npm run migrate` 필요**
- 변경 파일: 백엔드 1(`financeRoutes.js`) · 프런트 2(`refatrix-settlement.html`·`refatrix-finance.html`) · 마이그레이션 1 · 테스트 1(신규)
- **nav 캐시 마커**: `v20260902eps` (앞 작업과 동일)
- 검증: `node --check` 통과 · 인라인 `<script>` 0 errors · **신규 테스트 6/6** · 기존 순수 테스트 회귀 0건

---

## ① 증상

디렉터 보고: **"LUEMI 7,281 은 완납 처리됐는데 미배분 입금 통지가 같은 금액으로 2건 살아 있다. 지워지지 않는다."**

운영 API 실측(2026-09-02, 읽기 전용):

| 통지 | 금액 | 소진액(DB) | 잔여 | 상태 | 실제 배분 |
|---|---|---|---|---|---|
| #12 `luemi` | 7,280.57 | **6,798.95** | 481.62 | pending·부분배분 | **0건** |
| #13 `LUEMI` | 7,280.57 | **481.63** | 6,798.94 | pending·부분배분 | **0건** |
| #14 `luemi pago` | 7,280.57 | 0 | 7,280.57 | pending(신규) | 0건 |

| 인보이스 | 청구 | 입금 | 배분 건수 |
|---|---|---|---|
| folio 13 (#5) | 481.63 | **0** | **0건** |
| folio 25 (#19) | 6,798.95 | **0** | **0건** |

선수금(`/api/ar/advances`)도 **0건**. 즉 **반제·배분·선수금이 전부 사라졌는데 통지의 소진액만 유령으로 남았다.**
그 결과 통지가 「부분배분」으로 인박스에 영구히 남고, 취소·삭제는 `409 has_allocation` 으로 막혀 **손댈 방법이 없었다.**

## ② 원인 — 되돌리기 경로가 새 상태(pending 부분배분)를 몰랐다

`bdReleaseAmount` 의 레거시 분기:

```js
const link = SELECT deposit_id, amount FROM bank_deposit_payments WHERE payment_id=$1
if (!link) {
  UPDATE bank_deposits_pending
     SET status='pending', allocated_amount=0, payment_id=NULL, …
   WHERE payment_id=$1 AND status='allocated'      // ← 여기
  return …
}
```

`status='allocated'` 는 **0191 이전 세계의 가정**이다(통지는 닫히거나 열려 있거나 둘 중 하나).
0191 이 도입한 **부분배분 통지는 `status='pending'`** 이라 이 `WHERE` 에 한 행도 걸리지 않는다.
→ 소진액이 그대로 남고, 반제 헤더는 삭제되어 **되돌릴 수단까지 함께 사라진다.**

여기에 더해 소진액을 **증감(`- $2`)으로 관리**한 것이 상태를 고착시켰다. 한 번 어긋나면 스스로 회복할 길이 없다.

> **더 근본적인 원인은 통지 중복 등록이다.** 통장에는 7,280.57 이 **한 번** 들어왔는데 통지가 3건 등록됐고
> (송금인이 `LUEMI` / `luemi` / `luemi pago` 로 제각각 — 각각 따로 입력됐다),
> 영업지원이 서로 다른 통지에 반쪽씩 반제해 **어느 통지도 닫히지 않는** 그림이 만들어졌다.

## ③ 해결

### ⒜ 소진액은 «재계산»한다 — 원천은 링크행 하나

```js
// 소진액 = Σ bank_deposit_payments.amount (반제가 실존하는 링크만)
async function bdRecalcDeposit(c, depositId) { … JOIN sales_payments … }
```

- `bdReleaseAmount` 는 이제 링크만 정리하고 **소진액은 `bdRecalcDeposit` 이 다시 계산**한다.
- `JOIN sales_payments` 덕분에 **반제가 사라진 링크는 자동으로 빠진다** = 어긋나도 스스로 치유된다.
- 레거시(링크 없음)는 `payment_id` 포인터로 통지를 찾고, **status 조건을 두지 않는다.**
- FK 포인터(`payment_id`)는 상태와 무관하게 항상 먼저 끊는다(반제 헤더 DELETE 의 FK 위반 방지).
- 닫힘 판정은 `BD_REMAIN_EPS`(= `AR_PAID_EPS` = 0.5) 하나로.

### ⒝ 중복 통지 등록을 차단 — 근본 원인

- `POST /api/bank-deposits`: 같은 **계좌 · 입금일 · 금액**의 비-void 통지가 있으면 **409 `duplicate_suspect`**.
  정말 별개의 입금이면 화면이 `confirm_duplicate:true` 로 다시 보낸다.
- `GET /api/bank-deposits`: `dup_count` 추가.
- **수금/정산 인박스**: 금액 옆에 빨간 **「중복 의심 N건」** 칩(툴팁으로 설명).
- **재무 등록 화면**: 등록 시 확인창 — *"같은 돈을 두 번 등록하면 반제가 통지마다 쪼개져 어느 쪽도 닫히지 않습니다."*

### ⒞ 마이그레이션 0194 — 이미 어긋난 값 소급 정리

`pending`·`allocated` 통지의 소진액을 **링크행 합계로 재계산**한다. 규칙은 `bdRecalcDeposit` 과 동일.
`void`·`booked` 는 건드리지 않고, **거래(transactions)는 손대지 않는다** — 통지 등록은 원래 원장에 기표되지 않으므로
**계좌 잔액·현금흐름에 영향이 전혀 없다.** 값이 이미 맞으면 갱신하지 않는다(멱등).

---

## ④ 배포

앞 작업(`ar_paid_eps`)과 **같은 zip · 한 번에** 배포합니다. 단 이번엔 **마이그레이션이 있습니다.**

1. zip 압축 해제 → 경로 그대로 덮어쓰기
2. GitHub Desktop **Commit → Push**
3. **Railway** 자동 재배포 → Deployments **Success**
4. Railway APP 콘솔에서 **`npm run migrate`** 실행 (0194 적용) ← **필수. 이때 LUEMI 통지 2건이 정리된다.**
5. GitHub Pages 1~2분 → 수금/정산·재무 화면 **Ctrl+Shift+R** (nav `v20260902eps`)

## ⑤ 배포 후 정리 순서 (LUEMI)

1. **마이그레이션 직후** 인박스 확인 → 통지 #12·#13 의 「배분됨」이 **`—`**, 잔여 **7,281**, 「부분배분」 칩이 사라지고
   대신 **「중복 의심 3건」** 칩이 세 행 모두에 뜬다.
2. **재무 > 거래 등록** 탭 → 「미배분(대기) 입금」 → 중복 2건(`LUEMI`·`luemi`) **[삭제]**. (소진액이 0 이라 이제 허용된다)
3. 수금/정산 → 남은 `luemi pago` **[이 입금으로 반제]**
   → folio 13 에 **481.63**, folio 25 에 **6,798.94** 담고 → **[저장 · 통지 닫기]**
4. 결과: 통지 전액(7,280.57) 소진 → 인박스에서 사라짐. folio 13 완납.
   **folio 25 는 0.01 이 남는다**(청구 6,798.95 − 배분 6,798.94) → `AR_PAID_EPS` 덕분에 **「완납」** 으로 표시된다.
   ← 이 0.01 이 바로 같은 날 앞 작업에서 고친 "folio 25 가 잔액 0인데 연체" 의 정체다.

## ⑥ 테스트 (운영 스모크)

1. 통지 3건의 「배분됨」이 실제 배분과 일치하는지(0 이어야 한다).
2. 정상 반제 → 부분 배분 → 이어서 반제 → 전액 소진 자동 닫힘 (0191 회귀).
3. 반제 **취소** → 통지가 그만큼 되돌아오고 **「부분배분」이 정확히 풀리는지** ← 이번 수정의 핵심.
4. 배분 1건만 삭제 → 소진액이 그 금액만큼만 줄어드는지(전량 복구 아님).
5. 재무에서 같은 계좌·날짜·금액 재등록 → **확인창**이 뜨는지. [취소] 하면 등록 안 됨, [확인] 하면 등록됨.
6. 인박스에 **「중복 의심 N건」** 칩이 뜨는지.
7. 거래목록·계좌 잔액이 **마이그레이션 전후로 동일**한지(통지는 원장 미기표 — 변하면 안 된다).

## ⑦ 검증 (이번 세션)

- 운영 API **읽기 전용 조회**로 원인 규명(위 ① 표). **쓰기 작업은 하지 않았다.**
- `node --check` 통과 · 변경 HTML 2개 인라인 `<script>` 0 errors.
- **신규 `test/bank_deposit_alloc_recalc.test.mjs` 6/6 통과** — 옛 `status='allocated'` 조건 재발 가드 ·
  소진액 통째 0 덮어쓰기 금지 가드 · 링크 합계 재계산 + `JOIN sales_payments` · `BD_REMAIN_EPS` 사용 ·
  FK 포인터 선정리 · `duplicate_suspect`/`confirm_duplicate`/`dup_count` · 0194 멱등성 및
  **거래·삭제문 부재**(원장 무영향) · 화면 칩/확인창.
- 기존 순수 테스트 회귀: `ar_paid_eps` 7/7 · `ar.test.js` 8/8 · `logic.test.js` 24/24 — 새로 깨진 것 0건.

## ⑧ 핵심 학습 / 원칙

- **파생값은 증감시키지 말고 원천에서 다시 계산한다.** `allocated_amount` 를 `+`/`−` 로 관리한 순간,
  한 번의 누락이 영구 고착됐다. 링크행 합계로 재계산하니 같은 사고가 구조적으로 불가능해졌다
  — 09-01 `syncArPlanTxn` 과 정확히 같은 처방이다.
- **새 상태를 만들면 «되돌리는 경로»가 그 상태를 아는지 반드시 확인한다.** 0191 이 `pending` 부분배분을
  새로 만들었는데, 되돌리기 코드의 `WHERE` 는 옛 세계(`allocated`)에 머물러 있었다.
  08-31c 에서 "상태를 만드는 코드를 쓸 때 푸는 코드를 같이 쓴다"고 적어놓고 **또 같은 곳에서 당했다.**
- **막기만 하면 사용자는 가장 가까운 버튼을 누른다.** `409 has_allocation` 은 옳았지만 빠져나갈 정상 경로가 없었다.
  이번엔 마이그레이션(소급 정리) + 재계산(자동 치유) 으로 **막힌 상태 자체가 생기지 않게** 했다.
- **입력에서 막는 것이 가장 싸다.** 통지 중복 등록만 막았어도 이 사고 전체가 없었다.
  하류에서 정합성을 맞추는 코드 10줄보다 상류의 확인창 1개가 낫다.
- **신고를 그대로 실행하지 말고 먼저 읽는다.** "지워달라"를 곧이곧대로 했다면 통장에 실제로 들어온 7,280.57 의
  기록을 지웠을 수도 있었다. 통장 대조(한 번 입금) 확인이 삭제 대상을 «중복 2건» 으로 좁혀줬다.

## ⑨ 열린 메모

- **중복 탐지는 계좌·날짜·금액 완전 일치만** 본다. 하루 차이로 등록되거나 금액이 센타보 단위로 다르면 못 잡는다.
  필요해지면 ±1일 / ±1페소 범위로 넓히는 안 검토.
- 통지 **캡처 파일**(등록 시 필수)이 있으므로, 중복 의심 시 두 통지의 캡처를 나란히 보여주면 판단이 즉시 끝난다 — 다음 후보.
- 부분배분 통지의 취소·삭제는 여전히 반제를 먼저 되돌려야 한다(설계 의도). 다만 이제 되돌리기가 정확히 동작한다.
- 선수금 → 인보이스 배분을 되돌리면 선수금으로 환원되지 않는 문제(08-31c ⑧)는 **여전히 미해결**.

---
*이 문서는 인수인계용 스냅샷입니다. 큰 변경이 생기면 갱신해서 다시 올려주세요.*
