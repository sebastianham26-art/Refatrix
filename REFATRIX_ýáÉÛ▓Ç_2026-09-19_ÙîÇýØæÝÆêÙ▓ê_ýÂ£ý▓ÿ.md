# REFATRIX 점검 — 대응품번(경쟁사 코드)이 화면과 다른 이유 · 2026-09-19

## 1. 결론 — 두 화면이 서로 다른 표를 보고 있다

| | 보는 표 | 내용 |
|---|---|---|
| **제품/마케팅 → 제품 찾기** | `products.scode` | **SYD 만.** 한 칸에 ' // ' 로 여러 개 |
| **카탈로그 조회 API (종전)** | `product_xref_codes` | BAW · GROB · VASLO · KYB · MOOG · YOKOMITSU · SYD1 … |

코드로 확인한 사실이다.

- 제품 찾기 목록은 `SELECT p.code, p.scode, …` 로 **`scode` 를 그대로 화면에 찍는다**
  (`productRoutes.js` · `/api/products`). 교차참조표는 **읽지도 않는다.**
- 카탈로그 API 는 `referencias` 를 `product_xref_codes` 에서 뽑았다(`catalogPull.js`).

## 2. 그 교차참조표는 무엇인가

마이그레이션 `0130` 의 주석이 목적을 그대로 적고 있다.

> 경쟁사 교차참조 코드 (BAW / GROB / VASLO / KYB / MOOG / YOKOMITSU / SYD 추가분 등)
> · 목적: **어떤 경쟁사 코드를 입력해도 CTR 제품으로 역매칭** (현장재고조사 등)
> · 기존 product_syd_codes(SyD 전용)와 **별도** — SyD 매칭 경로는 그대로 두고…

즉 **「들어오는 코드를 알아보기 위한」 표**다. 커버리지 분석·개발 판단·현장 조사에서 쌓았고,
**고객에게 보여 줄 목록으로 검증된 적이 없다.** 화면과 개수도 브랜드도 다른 게 당연하다.

**내 판단이 틀렸던 지점:** 고객사 플랫폼이 「멀티브랜드 비교」라고 해서 교차참조표가 맞다고 보고
그쪽을 연결했다. 우리가 **검증해서 내보낼 수 있는 것**은 화면이 보여 주는 SYD 대응품번이라는 점을
먼저 확인했어야 했다.

## 3. 고친 것 — 화면과 같은 값만 나간다

`ref_source` 설정을 새로 뒀다. **기본값은 `scode`** — 제품 찾기 화면과 같은 값이다.

| 값 | 나가는 것 |
|---|---|
| **`scode`** (기본) | `products.scode` 를 ' // ' 로 쪼갠 것. 전부 `"marca": "SYD"` |
| `xref` | 종전 동작(교차참조표) |
| `both` | 둘을 합치고 같은 코드는 한 번만 |

`/brands` 도 같이 고쳤다. **실제로 내보내는 출처만 센다** — 기본 설정에서는 `SYD` 하나만 나온다.
전에는 목록에는 MOOG 가 뜨는데 응답에는 없는 상태가 될 수 있었다.

화면: 「응답 내용 → 대응품번 출처」. 「공개 브랜드」 칸은 교차참조표를 쓸 때만 의미가 있다.

## 4. 원천 데이터 점검 — `check_referencias.sql`

Railway 에서 실행한다(읽기 전용). 답이 나오는 질문:

| 블록 | 질문 |
|---|---|
| ① | 두 출처의 규모 — scode 보유 제품 수 vs 교차참조표 제품·코드 수 |
| ② | 교차참조표가 **어느 브랜드로** 채워져 있나 |
| ③ | 두 출처가 겹치나 (둘다 / scode만 / xref만 / 둘다없음) |
| ④ | 샘플 10건 — 화면 값과 교차참조표 값을 **나란히** |
| ⑤ | 화면에는 있는데 교차참조표에는 없는 SYD 코드 |
| ⑥ | scode 형식 분해(숫자형 vs 영문·기호 포함) |
| ⑦ | 한 제품에 SYD 코드가 여러 개인 경우 |
| ⑧ | **고객에게 실제로 나갈 대상**(PRO 제외) 중 대응품번이 있는/없는 제품 수 |

**⑧ 이 가장 중요하다.** 대응품번이 없는 제품은 고객 플랫폼에서 비교 대상이 잡히지 않는다.

6월 엑셀(`★ CTR VS SYD BASE`) 기준으로는 1,688개 제품 **전부** SYD 코드를 갖고 있었고,
그중 숫자형 1,477 · OEM 형(`6Q0-407-365-A` 등) 254 · 복수 보유 41개였다. ERP 현재 값도
그 수준인지 ⑥⑦ 로 확인하면 된다.

## 5. 계약서에 생기는 문제 — 결정이 필요하다

계약서는 **멀티브랜드 비교**를 전제로 쓰여 있다.

- §1 「여러 브랜드의 대응 품번을 한 화면에서」
- §7 「레파트릭스는 사전 통보 없이 **브랜드를 추가**할 수 있으므로 목록을 동적으로 받아라」
- 제목 자체가 `perfil Comparador Multimarca`

이제 실제로 나가는 것은 **SYD 대응품번뿐**이다. 고객사 플랫폼이 멀티브랜드인 것과,
**우리가 주는 데이터가 멀티브랜드인 것은 다른 이야기**다. 그대로 보내면 개발자가 여러 브랜드가
올 것으로 짜고, 실제로는 SYD 만 오는 상태가 된다.

두 가지 중 하나를 골라야 한다.

1. **계약서를 SYD 기준으로 고친다** (권장) — §1·§7 을 「CTR ↔ SYD 대응품번」으로 바꾸고,
   브랜드가 늘어나면 그때 통보한다고 적는다. `referencias` 구조(`marca`+`codigo`)는 그대로 두면
   나중에 브랜드를 늘려도 연동을 안 고쳐도 된다.
2. **교차참조표를 검증해서 내보낸다** — 브랜드별로 우리가 책임질 수 있는 수준인지 확인하는
   작업이 먼저다. ②④⑤ 결과를 보고 판단할 일이다.

말씀 주시면 국문·스페인어 계약서를 그에 맞게 고쳐 드리겠다.

## 6. 적용

1. 이 묶음을 덮어쓰기 → 커밋 · 푸시
2. Railway 에서 **`npm run migrate`** (0224)
3. Ctrl+Shift+R → 고객사 설정에 **「대응품번 출처」** 칸 확인
4. **「미리보기 — 고객이 받을 값」** 으로 `referencias` 가 화면 값과 같은지 눈으로 확인

시험 39개 전부 통과(실 PostgreSQL 포함). 새로 넣은 것:

```
기본 출처가 scode 다                                  ✅
scode 를 배열로 쪼갠다(중복 제거·OEM 형식 보존)        ✅
기본 설정에서 MOOG(교차참조표)는 나가지 않는다         ✅
출처를 xref 로 바꾸면 MOOG 가 나온다(되돌릴 수 있다)   ✅
/brands 가 실제 내보내는 브랜드만 센다                 ✅
```
