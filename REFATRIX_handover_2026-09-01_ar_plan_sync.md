# REFATRIX 인수인계 — 완납된 인보이스의 「매출 입금예정」 자동 정리 (RECAR 24,005 오진 사건)

- 작업일: 2026-09-01
- 마이그레이션: **0192** (신규 · `0192_ar_plan_sync_backfill.sql`) → `npm run migrate` **필요**
- 변경 파일: 백엔드 2(`financeRoutes.js` · `notaCreditoRoutes.js`) · 마이그레이션 1 · 테스트 1(신규) · **프런트 변경 없음**
- 산출물 zip: `refatrix_20260901_ar_plan_sync.zip`
- 검증: 실 PostgreSQL 16 · 0001~0192 · **종단 9건 전부 통과**(3회 연속) · 전체 스위트 회귀 **0건**

> ⚠️ **선행 조건**: 2026-08-31 패키지(`refatrix_20260831_bd_partial.zip`, 마이그레이션 0191)가 **먼저** 적용돼 있어야 합니다.
> 이 작업은 그 위에서 같은 `financeRoutes.js` 를 이어서 고칩니다.
> (그리고 2026-08-27 `ar_paid_eps` 미반영 경고는 여전히 유효 — 그 패키지를 살릴 거라면 이 모든 것보다 먼저.)

---

## ① 무엇이 문제였나 — 신고와 실제가 달랐다

**신고(디렉터)**: "recar, **24,005가 선수금으로 남아있는데** 이걸 지워줘. folio 31에 입금을 반제처리하고 완납기록이 이상없이 되어 있어."

**운영 DB 조회 결과(읽기 전용) — 신고와 달리 RECAR 데이터는 정상이었다:**

| 항목 | 값 | 판정 |
|---|---|---|
| 입금통지 #11 | 24,005 · 08-26 · memo `ryo_recar` | `allocated` — 정상 닫힘 |
| 입금건 #26 | 입금 24,005 → 인보이스 #28 에 **24,004.53** 배분 | 정상 |
| folio 31 (인보이스 #28) | 청구 24,004.53 / 입금 24,004.53 / **잔액 0 · 완납** | 정상 |
| 거래 #2067 | 매출수금(4010) 24,004.53 · BBVA · 실적 | 정상 |
| 거래 #2068 | **선수금(2030) 0.47** · BBVA · 실적 | RECAR 의 실제 선수금은 **0.47** |

24,005 = 24,004.53 + 0.47 로 정확히 떨어지고 통장과 원장이 일치. 이중계상 없음.
전 시스템 선수금 잔여 입금건도 3개뿐 — LUEMI 6,798.94 / LUEMI 481.62 / RECAR **0.47**. **24,005 짜리 선수금은 존재하지 않았다.**

**그럼 디렉터가 본 24,005 는 무엇이었나:**

> 거래 **#1950 · 「매출 입금예정 (인보이스 #28)」 · 24,004.53 · 2026-08-30 · 계좌 미지정 · status=`plan`**

인보이스 발행 시 총액으로 자동 생성되는 **수금예정(AR plan) 거래**가, **완납된 뒤에도 거래목록에 그대로 남아 있었다.**
금액이 24,004.53 이라 24,005 로 읽히고, folio 31 은 완납인데 돈이 떠 있는 것처럼 보였다.

**그리고 RECAR 만이 아니었다** — 완납인데 수금예정이 살아있는 건이 **5건**:
RECAR folio 31 (24,004.53) · MARTINEZ folio 26 (1,253.01) · LUEMI folio 18 (3,037.34) · folio 14 (1,477.84) · folio 12 (3,574.66).

**원인**: 예정 거래를 **줄이거나 닫는 코드가 어디에도 없었다.**
`/api/cashflow` 만 조회 시점에 `adjustArPlansToOutstanding()` 으로 보정했고(2026-08-06), **거래목록·예정 내역은 원본을 그대로** 보여줬다. 게다가 계획삭제 경로(`POST /api/transactions/plans/delete`)는 매출 연계 예정을 `sales_linked` 로 **의도적으로 거부**한다("매출에서 처리") — 그런데 정작 매출 쪽에 처리하는 코드가 없었다.

## ② 해결 — 반제할 때마다 예정을 잔액에 맞춘다

`financeRoutes.js` 에 공통 헬퍼 **`syncArPlanTxn(c, invoiceId)`** 신설(export):

```
잔액 = posted·미삭제 인보이스면 total_mxn − Σ배분, 아니면 0
 · 잔액 < AR_PLAN_EPS(0.5) → 예정 거래 **소프트 삭제**(deleted_at)
 · 잔액이 남으면          → 예정 금액을 **잔액으로 갱신** (삭제됐던 건 되살림)
```

- **소프트 삭제**라 `sales_invoices.txn_id` FK 는 그대로. 되돌리면 그대로 부활한다.
- `plan_amount` 는 **손대지 않는다**. AR 예정은 애초에 `plan_amount` 가 NULL 이라 **계획대비실적 계획선에 원래 잡히지 않는다**(`planVsActual` 은 `plan_amount_mxn` 만 본다) → 그 리포트에 영향 0.
- `/api/cashflow` 의 기존 보정은 같은 공식이라 이제 **멱등한 no-op** 이 된다. 두 화면 기준이 비로소 일치.
- 임계값 0.5 는 `AR_PAID_EPS`·`BD_REMAIN_EPS` 와 같은 취지(화면 정수 반올림 기준).

**호출 지점 — 배분이 바뀌는 모든 경로:**

| 경로 | 시점 |
|---|---|
| `POST /api/ar/payments` | 배분 insert 후 (배분한 인보이스마다) |
| `POST /api/ar/advances/:id/apply` | 선수금 배분 후 |
| `DELETE /api/ar/payments/:id` | 배분 삭제 후 — **예정 복구** |
| `DELETE /api/ar/allocations/:id` | 배분 1건 삭제 후 |
| `PATCH /api/ar/allocations/:id` | 금액 수정 후 |
| `POST /api/nc/:id/apply` · `/void` · `DELETE /api/nc/:id` | NC 비현금 반제 적용·취소·삭제 후 |

### 마이그레이션 0192 — 이미 쌓인 건 소급 정리

라우트가 붙기 **전에** 생긴 잔류 예정을 한 번 정리한다(위 5건 포함). 규칙은 `syncArPlanTxn` 과 동일:
완납 건은 소프트 삭제, 부분수금 건은 잔액으로 축소, **미수 건은 손대지 않음**. 두 번 돌려도 결과가 같다(멱등).

---

## ③ 배포

1. **선행**: 0191 패키지가 이미 적용돼 있는지 확인(`bank_deposits_pending.allocated_amount` 컬럼 존재).
2. zip 압축 해제 → 경로 그대로 덮어쓰기 (`migrations/0192…` · `src/routes/financeRoutes.js` · `src/routes/notaCreditoRoutes.js` · `test/ar_plan_sync.test.mjs`).
3. Commit → Push → Railway **Success**.
4. Railway APP 콘솔 **`npm run migrate`** (0192 적용) ← **이때 5건이 정리된다.**
5. **프런트 재배포 불필요** (화면 변경 없음, nav 마커 그대로).

검증:
```
curl -s ".../main/refatrix-api/src/routes/financeRoutes.js?nc=$(date +%s)" | grep -c "syncArPlanTxn"   # 6+
curl -s -o /dev/null -w "%{http_code}\n" ".../main/refatrix-api/migrations/0192_ar_plan_sync_backfill.sql"  # 200
```

## ④ 테스트 방법 (운영 스모크)

1. **마이그레이션 직후** 재무 > 거래목록에서 **RECAR / 「매출 입금예정 (인보이스 #28)」 24,004.53 이 사라졌는지** ← 이번 신고의 해결 확인.
2. 나머지 4건(MARTINEZ folio 26 · LUEMI folio 18/14/12)의 예정도 사라졌는지.
3. **미수 인보이스의 예정은 그대로 남아 있는지**(회귀 — 이게 사라지면 안 된다).
4. 부분수금된 인보이스가 있으면 예정 금액이 **잔액으로** 줄어 있는지.
5. 새 반제 1건 실행 → 완납되면 그 인보이스 예정이 즉시 사라지는지.
6. 그 반제를 **취소** → 예정이 **총액으로 되살아나는지**.
7. 현금흐름·워터폴의 「매출입금 예정」 합계가 **수금/정산 수금잔액과 여전히 일치**하는지(보정이 no-op 이 됐을 뿐 값은 동일해야 한다).

## ⑤ 검증 (이번 세션)

- 운영 DB **읽기 전용 조회**로 원인 규명(위 ① 표). **쓰기 작업은 하지 않았다.**
- `node --check` 백엔드 2파일 통과.
- **신규 `refatrix-api/test/ar_plan_sync.test.mjs` — 9건 전부 통과**
  ① RECAR folio 31 재현(24,004.53 인보이스 + 24,005 통지 → 선수금 0.47 · **예정 소멸**)
  ② 부분수금 → 예정이 잔액 13,200 으로 축소 / ③ 반제 취소 → 예정 총액 복구
  ④ 선수금 배분으로 완납 → 예정 소멸 / ⑤ NC 적용 → 소멸, NC 취소 → 복구
  ⑥ **0192 백필**: 라우트를 우회해 만든 "구 데이터"(완납·부분수금)를 정리, **두 번 실행해도 동일**(멱등)
  ⑦ 미수 인보이스 예정은 무변경(회귀)
  - 3회 연속 재실행 9/9.
- 같은 DB 에서 이번 세션 4개 파일 연속 실행: **13 + 14 + 9 + 9 = 45건 전부 통과.**
- **전체 스위트 회귀**: 변경 전(HEAD)·후를 각각 새 DB 에 마이그레이션해 비교 — 실패 목록 동일, **새로 깨진 테스트 0건**.

## ⑥ 핵심 학습 / 원칙

- **"조회할 때 보정"은 절반의 해결이다.** `/api/cashflow` 만 보정하고 원본을 두니, 보정하지 않는 화면(거래목록)에서 같은 데이터가 다르게 보였다. 사용자는 그 차이를 **버그가 아니라 돈 문제로** 읽는다 — 이번엔 "선수금 24,005"라는 존재하지 않는 항목으로 신고됐다.
- **신고 내용을 그대로 실행하지 말 것.** "선수금 지워줘"를 곧이곧대로 했다면, RECAR 의 유일한 입금건(#26)을 지워 **folio 31 완납이 깨지고 24,005 가 원장에서 사라졌을 것**이다. 지우기 전에 데이터를 본 것이 유일하게 옳은 순서였다.
- **차단만 하고 대안을 안 만들면 상태가 고인다.** 계획삭제가 매출 예정을 `sales_linked` 로 막으면서 "매출에서 처리"라고 적어뒀지만 그 처리 코드가 없었다. **거부에는 반드시 짝이 되는 정상 경로가 있어야 한다.**
- **파생 데이터는 원천이 바뀌는 모든 지점에서 갱신한다.** 배분이 바뀌는 곳이 7군데였다 — 한 곳이라도 빠지면 다시 어긋난다. 헬퍼 하나로 모으고 호출부를 표로 관리.
- 소프트 삭제 + 복구 가능하게 설계하면, 파생 데이터 정리가 **되돌릴 수 없는 작업이 아니게 된다**.

## ⑦ 열린 메모

- **RECAR 0.47 선수금은 그대로 둔다**(디렉터 확정). 실제로 통장에 들어온 돈이라 원장에 남는 게 맞다. 센타보 선수금이 쌓이면 「소액 잔액 정리(write-off)」 기능을 만든다 — 2026-08-27 문서에서 예고된 그 항목.
- **LUEMI 선수금 2건**(6,798.94 · 481.62, 배분 0건)은 그대로 남아 있다. 08-31 패키지 배포 후 「선수금(과입금)」 카드에서 배분하거나 취소하면 된다.
- 선수금 → 인보이스 배분을 되돌리면 선수금으로 환원되지 않는 문제(08-31c 문서 ⑧)는 **여전히 미해결**.
- 거래목록의 `LIMIT 200` 고정 — 기간을 넓게 잡으면 조용히 잘린다. 이번 조사에서도 한 번 물렸다(2027년까지 조회하니 8월 데이터가 통째로 빠짐). 페이지네이션이나 경고 표시 검토.

---
*이 문서는 인수인계용 스냅샷입니다. 큰 변경이 생기면 갱신해서 다시 올려주세요.*
