# REFATRIX 인수인계 — 재무 > 커미션에 「성과급(Bono)」 + 목표 대비 진척

- 작업일: 2026-08-28
- 마이그레이션: **0190** (신규 · `0190_commission_bonus.sql`)
- **nav.js 변경 없음** → 다른 HTML 의 nav 토큰 일괄 변경 **불필요**
- 산출물: `refatrix_bono_v1.zip` (6파일 · 레포 구조 그대로 → 루트에서 풀면 제자리)
- 검증: **순수 18 + 종단(실 PG16 · 0001~0190) 12 + 기존 커미션 회귀 13 + npm test 24 = 전부 통과**, 전체 스위트 회귀 0건

---

## ① 무엇을 만들었나

커미션 화면을 **커미션(율 기반)** 과 **성과급(Bono · 목표 달성률 기반)** 두 축으로 나누고,
**매출목표 대비 매출 / 수금목표 대비 수금**을 당일 기준으로 나란히 보여준다. 디렉터·재무 화면과 **영업사원 본인 화면 모두**에 적용된다.

### (1) 이번 달 진척 — 화면 최상단 (전 역할 공통)

매출·수금 두 패널을 나란히. 각 패널에 목표·실적·달성률, **월 경과율 마커**(검은 선 — 막대가 이 선보다 앞서면 페이스를 앞선 것),
남은 목표, 페이스 %, 월말 예상액. 성과급 기준 패널은 보라 테두리, 커미션 기준 패널에는 `커미션 기준 4%` 배지.

그 아래 KPI 4개 — **기대 커미션(당월 인식분) / 기대 성과급(현재 달성률) / 월말 예상 성과급(페이스 유지 시) / 당월 기대 보상 합계**,
그리고 “수금 **$40,000** 를 더 채우면 달성률 80% 구간(**$2,000**)에 도달합니다” 안내.

### (2) 수금목표는 시스템이 산출한다 ★ 핵심

```
만기일        = sales_invoices.due_date  (없으면 inv_date + credit_days)
당월 만기도래 = Σ subtotal(ex-IVA)  WHERE 만기일 ∈ 그 달
연체 이월     = Σ 미수잔액(ex-IVA)  WHERE 만기일 < 월초 AND 월초 시점 미수
수금목표      = 당월 만기도래 + (옵션) 연체 이월
```

- 고객 결제조건(외상일)에서 파생되므로 **영업사원이 손댈 수 없다 — 목표 조작 여지가 없다.**
- 옵션 `당월 만기 + 연체 이월` / `당월 만기도래분만` 을 사원별로 고른다.
- **미래 월은 잠정치**(`잠정` 배지) — 아직 발행되지 않은 매출의 만기가 빠져 있어, 그 달이 시작될 때 확정된다. 미래 월에는 연체 이월을 더하지 않는다.
- **매출목표만** 디렉터가 월별로 직접 입력한다(자동채우기: 시작월 목표 + 월 증가율).

### (3) 성과급 = 달성률 구간별 정액(계단식)

`80%↑ 2,000 / 100%↑ 5,000 / 120%↑ 10,000` 처럼 구간을 정하면, 달성률이 하한 이상인 구간 중 **가장 높은 것 하나**를 정액 지급.
최저 구간 미만이면 0. 기준(매출/수금)은 사원마다 하나 고정이며 **커미션 기준과 달라도 된다**(예: 커미션은 수금 4%, 성과급은 매출 목표).
`성과급 미사용` 도 있다 — 100% 커미션 사원처럼 목표 개념이 없는 경우.

### (4) 실적 조회 — 영업사원 본인 화면의 본체

`시작월 · 종료월 · 고객` 필터 + 보기 전환 **월별 / 고객별 / 인보이스**.

| 보기 | 열 |
|---|---|
| 월별 | 매출(목표·실적·달성률) · 수금(목표`자동`·실적·달성률) · 커미션 · 성과급 · 합계 · 상태 |
| 고객별 | 결제조건 · 매출 · 만기도래 · 수금 · **미수잔액** · **연체** · 커미션 |
| 인보이스 | 발행일 · 매출(ex-IVA) · 만기일 · 수금일 · 상태(수금완료 / 지연수금 +N일 / 연체 +N일 / 만기 D-N) · 커미션 |

영업사원은 **본인 것만** 보인다(서버가 `agent_id` 를 무시하고 본인으로 강제). 고객 필터가 걸리면 성과급은 표시하지 않는다(월 전체 실적 기준이라).
영업사원 화면 하단 `내 커미션 조건` 카드에 **내 성과급 조건**(구간표 + 기준 + 적용기간)이 함께 나온다.

### (5) 확정·지급 — 기존 흐름에 얹었다

- 디렉터가 **월 확정**(기존 [확정] 버튼)을 누르면 그 달 성과급이 `bonus_payouts` 에 **스냅샷으로 동결**된다(목표·실적·달성률·금액). 이후 구간·목표를 바꿔도 확정된 달은 불변, **미확정 월만 재계산**.
- **지급 전표 1건이 커미션 + 성과급을 함께 반제**한다. 커미션 라인을 FIFO 로 충당하고 **남는 금액으로 성과급(월 1건)** 을 충당(부분충당 없음). 지급 패널에 `＋ 성과급(Bono) 1건 — 목표/실적 → 금액` 이 표시되고 기본 지급액에 포함된다.
- 커미션 라인이 없고 성과급만 있는 달도 지급할 수 있다.

---

## ② 배포 — ⚠ 순서 준수

| 파일 | 위치 | 구분 |
|---|---|---|
| `0190_commission_bonus.sql` | `refatrix-api/migrations/` | **신규** |
| `commissionBonus.js` | `refatrix-api/src/routes/` | **신규** |
| `commissionRoutes.js` | `refatrix-api/src/routes/` | 덮어쓰기 (+40 / −10줄) |
| `refatrix-commission.html` | 레포 루트 | 덮어쓰기 |
| `commission_bonus.test.js` · `commission_bonus_e2e.test.mjs` | `refatrix-api/test/` | (선택) 검증용 |

1. zip 을 레포 루트에서 풀어 반영.
2. GitHub Desktop → **Commit → Push** → Railway 자동 재배포 **Success** 확인.
3. **🔴 Railway APP 콘솔에서 `npm run migrate`** → `apply 0190_commission_bonus.sql` 확인.
   - 0190 을 안 돌려도 **500 이 나지 않는다**(백엔드가 테이블 없음을 감지해 화면에 “npm run migrate (0190)” 안내만 띄우고 커미션은 정상 동작 — 배포 순서 안전성 검증 완료). 다만 성과급은 저장·계산되지 않는다.
4. 커미션 화면 **Ctrl+Shift+R**. 상단에 `build b0828bono` 마커가 보이면 새 화면.

**배포 확인 (raw URL)**
```bash
R=https://raw.githubusercontent.com/sebastianham26-art/Refatrix/main
curl -s -o /dev/null -w "%{http_code}\n" "$R/refatrix-api/migrations/0190_commission_bonus.sql"   # 200
curl -s "$R/refatrix-api/src/routes/commissionRoutes.js" | grep -c "registerBonusRoutes"          # 2
curl -s "$R/refatrix-api/src/routes/commissionBonus.js"  | grep -c "colTarget"                    # >0
curl -s "$R/refatrix-commission.html" | grep -c "b0828bono"                                       # >0
```

---

## ③ 테스트 방법 (운영 스모크 · 10분)

**디렉터**
1. 커미션 화면 상단 **[성과급(Bono) 설정]** → 한 사원 체크 → 기준 `수금 목표` · 적용 `2026-09 ~ 2027-02` · 구간 `80/100/120` → **저장**.
2. 상단 **영업사원** 드롭다운에서 그 사원 선택 → **이번 달 진척**에 매출·수금 두 패널과 경과율 선이 보이는지. 수금 패널에 `목표 자동산출` 배지와 `성과급 기준` 테두리가 있는지.
3. **실적 조회** → 월별/고객별/인보이스 전환. 월별에서 수금목표가 만기도래액과 맞는지(고객별 보기의 `만기도래` 열과 대조).
4. 기준을 `매출 목표` 로 바꾸고 **자동채우기**(예: 200,000 + 10%) → 저장 → 월별 표의 매출 달성률 열이 강조되는지.
5. 월 확정([확정] 버튼) → 그 달 성과급이 `확정` 배지로 바뀌고, 이후 구간을 바꿔도 **금액이 그대로**인지.

**재무담당**
6. 지급 패널에서 그 달 선택 → `＋ 성과급(Bono) 1건` 이 뜨고 기본 지급액이 **커미션 + 성과급 합계**인지 → 증빙 첨부 후 지급 등록 → “커미션 N건 … · 성과급 …” 메시지, 성과급이 지급완료로 바뀌는지.

**영업사원 로그인**
7. 진척 패널·실적 조회가 **본인 것만** 보이는지, `내 성과급(Bono) 조건` 구간표가 보이는지. 타인 데이터가 안 나오는지.

---

## ④ 변경 요약 (기술)

**스키마 (0190)**

| 테이블 | 용도 |
|---|---|
| `bonus_plans` | 사원 1명당 1건 — `basis(revenue/collection)` · `start_month/end_month` · `include_overdue` · `partial_credit` |
| `bonus_targets` | `(user_id, month) → revenue_target` — **매출목표만** 저장(수금목표는 저장하지 않고 산출) |
| `bonus_tiers` | `(user_id, min_rate) → amount` — 달성률 구간 |
| `bonus_payouts` | `(user_id, settle_ym)` 월 확정 스냅샷 — 목표·실적·달성률·구간·금액 + 지급 추적(`payment_id`) |

커미션 쪽 테이블(`commission_agent_periods` 등)은 **한 줄도 건드리지 않았다.**

**백엔드**
- **신규 `commissionBonus.js`** — 순수 계산(`buildPerf` · `pickTier` · `nextTier` · `validateBonusPlan` · 월 유틸)과 라우트 등록. 무거운 로직을 새 파일에 두어 `commissionRoutes.js` 의 변경을 최소화했다.
  - 신규 엔드포인트: `GET /api/commission/progress` · `GET /api/commission/performance?view=…&from&to&customer_id` · `GET|POST /api/commission/bonus/plans[/:uid]` · `GET /api/commission/my-bonus`
  - 열람 범위: 영업사원=본인 강제 / 디렉터·재무·소시오=지정 사원. 정책 저장은 디렉터만.
- **`commissionRoutes.js` (+40/−10)** — ① `registerBonusRoutes(app)` 호출 ② 월 확정 시 `snapshotBonusForMonth` ③ `/payable` 응답에 `bonus`·`commission_total` 추가 ④ `/payments` 가 FIFO 잔액으로 성과급 반제 ⑤ **덤으로 고친 기존 버그**: 배치·전표 목록의 `pay_date`/`paid_date` 가 `Sat Aug 15` 로 깨지던 것을 `to_char` 로 고정.

**금액 기준** — 전부 **IVA 제외(ex-IVA)**. 수금액은 충당액 × `subtotal/total` 로 ex-IVA 환산하며, `partial_credit=true`(기본)면 **부분수금도 비례 인정**, false 면 완납분만 인정(커미션 확정 규칙과 동일).

**프런트 `refatrix-commission.html`** — 진척 2패널 · 실적 조회(월별/고객별/인보이스) · 성과급 설정 모달 · 영업사원용 `내 성과급 조건`. 영업사원 선택 드롭다운을 최상단 카드로 옮겨 진척·조회·내역이 같은 선택을 공유한다.

---

## ⑤ 검증 결과

- 실 **PostgreSQL 16** 에 마이그레이션 **0001~0190 전체 클린 적용**.
- **순수 로직 18** — 월 유틸·경과율, 구간 판정(경계 79.9/80/119.99/120), 정책 검증(중복구간·종료월 역전·매출기준 목표 누락), 수금목표 산출, 연체 이월 on/off, 미래 월 잠정, 부분수금 비례 vs 완납기준, 완납월 인식, 지급분 동결, 기간 없는 인보이스, 고객별 집계, 인보이스 상태, 고객 필터, 목표 0 나누기 방지.
- **종단 12 (app.inject + 실 PG)** — 정책 저장 권한(영업사원 403), 수금목표 자동산출, 커미션·성과급 계산, 진척(경과율·다음 구간), **권한 격리(남의 id 넣어도 본인으로 강제)**, 월 확정 스냅샷 동결, 지급 대상에 성과급 포함, 전표 1건이 커미션+성과급 반제, 성과급 미사용 사원, 기존 `overview` 회귀.
- **브라우저 실동작** — 디렉터·영업사원 두 역할로 실제 서버에 붙여 화면 렌더·모달 저장·뷰 전환까지 확인, **JS 오류 0**.
- **회귀** — 기존 `commission_periods` 13건, `npm test` 24건 통과. 전체 스위트 실패 건수 변경 전후 **동일(57건, 모두 TEST_PG_URL 등 환경 미설정으로 인한 기존 skip성 실패)**.
- **배포 순서 안전성** — 0190 미적용 DB 에서 커미션·성과급 엔드포인트 8종 전부 **200**(500 없음) 확인.

---

## ⑥ 정책상 남은 결정 (필요하면 다음 세션에서)

1. **부분수금 인정** — 기본을 `부분수금 비례 인정` 으로 두었다(현금 회수를 측정하는 지표라). 커미션처럼 완납분만 인정하려면 사원별 `수금 인정` 을 `완납분만` 으로 바꾸면 된다.
2. **수금 기준 구간 하한** — 월말에 만기가 몰린 인보이스는 익월 초에 들어오는 게 정상이라 수금 달성률은 구조적으로 매출보다 낮게 나온다. 수금 기준 사원은 `80/100/120` 대신 **`75/90/105`** 쪽이 현실적이다.
3. **결제조건 미입력 고객의 기본 외상일** — 현재는 `customers.credit_days` 기본값 0 → 발행일 당일 만기로 잡힌다. 0 인 고객이 있으면 수금목표가 앞당겨진다. 고객마스터 점검 권장.
4. 신용메모·반품 시 확정된 수금목표를 재산출할지(현재 스냅샷 유지), 달성률 **상한(cap)** 을 둘지.

> 참고: 월 확정 시 감사로그에 `[audit] logEvent failed: confirm … audit_log_action_check` 경고가 남는다. **이번 작업 이전부터 있던 것**으로(`action='confirm'` 이 체크 제약에 없음), 기능에는 영향이 없다. 정리하려면 `audit_log_action_check` 에 `confirm`·`hand_off` 를 추가하면 된다.
