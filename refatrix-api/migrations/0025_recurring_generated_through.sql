-- =====================================================================
-- Refatrix ERP · 0025_recurring_generated_through
-- 규칙별로 "어디까지 예정거래를 생성했는지" 기록.
-- 최초 12개월분 생성 후, 이후엔 이 날짜 다음부터 사용자가 고른 개월수만큼만 이어서 생성.
-- 오늘 기준 24개월을 넘는 구간은 생성하지 않음(상한).
-- =====================================================================

ALTER TABLE recurring_rules ADD COLUMN IF NOT EXISTS generated_through DATE;
