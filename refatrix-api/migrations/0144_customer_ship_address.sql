-- =====================================================================
-- Refatrix ERP · 0144_customer_ship_address
--   고객 배송지(등록 주소와 다를 수 있음). 포장완료 후 출력하는
--   박스 라벨(etiqueta)·패킹리스트에 인쇄하기 위한 자유 텍스트 컬럼.
--   · 승인 플로우 없이 언제든 입력 가능(customerRoutes.js 즉시저장 엔드포인트).
--   · 길이 제한 없음(TEXT) — 주소가 길어도 잘리지 않게 라벨 양식에서 자동 줄바꿈.
-- =====================================================================

ALTER TABLE customers ADD COLUMN IF NOT EXISTS ship_address TEXT;
