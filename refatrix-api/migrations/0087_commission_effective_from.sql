-- =====================================================================
-- 커미션 적용 시작일 (영업사원별)
--   디렉터가 영업사원마다 "이 날짜부터의 매출만 커미션 대상" 시점을 지정.
--   예) effective_from = 2026-07-01 → 7/1 이상(inv_date >=) 매출만 커미션 계산,
--       6월 이전 매출은 기대/확정/지급 어디에도 잡히지 않음.
--   NULL = 제한 없음(전체 기간 매출이 커미션 대상) — 기존 동작 유지.
-- =====================================================================
ALTER TABLE commission_agents ADD COLUMN IF NOT EXISTS effective_from DATE;
