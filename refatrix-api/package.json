{
  "name": "refatrix-api",
  "version": "0.1.0",
  "description": "Refatrix ERP backend skeleton (Fastify + PostgreSQL)",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node test/logic.test.js",
    "migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "@fastify/jwt": "^8.0.1",
    "fastify": "^4.28.1",
    "pg": "^8.12.0"
  }
}
