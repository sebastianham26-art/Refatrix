# REFATRIX 인수인계 — 수금/정산에 「완납일」 + 「만기 대비 지연」 표시

- 작업일: 2026-08-31 (같은 날 `bank_deposit_partial_alloc` 작업에 **이어서**)
- **마이그레이션 없음** (0191 은 앞 작업 것 — 같은 zip 에 함께 들어있음)
- 변경 파일: 백엔드 1(`financeRoutes.js`) · 프런트 1(`refatrix-settlement.html`) · 테스트 1(신규)
- **nav 캐시 마커**: `v20260831bd` (앞 작업과 동일 — 추가 상향 없음)
- 산출물 zip: **`refatrix_20260831_bd_partial.zip`** — 앞 작업(0191 부분배분)과 **한 zip 으로 합쳐 배포**합니다. 두 작업이 같은 파일 2개를 건드리므로 나눌 수 없습니다.
- 검증: 실 PostgreSQL 16 · 0001~0191 · **종단 7건 + 프런트 순수 7건 = 14건 전부 통과**(2회 연속 재실행 포함) · 전체 스위트 회귀 **0건**

---

## ① 무엇을 · 왜

디렉터 요청: **"수금/정산에서 실제 수금액이 100% 들어온 날을 표기하고, due date 에서 얼마나 지연되었는지 표시"**

기존 화면은 완납 건에 초록 「완납」 칩만 붙었을 뿐, **언제** 다 받았는지도, **만기보다 얼마나 늦게** 받았는지도 알 수 없었다.
수금 성과(정시납 여부·평균 지연)를 화면에서 바로 볼 수 없어 고객별 수금 습관을 판단할 근거가 없었다.

## ② 정의 (디렉터 확정 2026-08-31)

| 항목 | 정의 |
|---|---|
| **완납일** `settled_date` | 인보이스 잔액이 0이 된 날 = **마지막 반제일**. 현금 반제는 `sales_payments.pay_date`, NC(비현금)는 `notas_credito` 의 적용일(`applied_at` → 없으면 승인일 → 작성일) |
| **지연** `settled_delay` | `완납일 − 만기일`. **양수 = 지연 · 0 = 정시 · 음수 = 조기** |
| **NC 표시** `settled_has_nc` | 반제에 NC(비현금)가 섞였으면 완납일 옆에 **「NC」 칩**. 현금으로 100% 받은 게 아니라는 뜻 |

> **왜 마지막 반제일인가**: "100% 들어온 날"은 마지막 1원이 들어온 날이다. 중간 부분수금일(예: 7/20에 50%)은 완납일이 아니다.
> **왜 NC 를 포함하되 칩으로 드러내는가**: NC 로 마감된 건을 완납일 「—」로 비우면 수금 성과 통계에서 통째로 빠져 왜곡된다. 날짜는 잡되 현금 100%가 아님은 칩으로 정직하게 보여준다.

**중요**: 서버는 미수 인보이스에도 `settled_date`(= 마지막 부분수금일)를 실어 보내지만, **화면은 완납 건에만 표시**한다(`arSettledDate()` 가 `paid_full` 을 검사). 미수 건에 "완납일"이 찍히면 오독의 소지가 크기 때문.

## ③ 변경 내용

### 백엔드 `refatrix-api/src/routes/financeRoutes.js` (마이그레이션 없음)

- 공통 조인 상수 **`AR_SETTLED_SQL`** 신설 — 세 엔드포인트가 같은 정의를 쓰도록 한 곳에 둔다(완납 판정 임계값을 `AR_PAID_EPS` 로 모았던 것과 같은 원칙).
  ```sql
  LEFT JOIN (
    SELECT al.invoice_id,
           MAX(COALESCE(p.pay_date, nc.applied_at::date, nc.approved_at::date, nc.created_at::date)) AS settled_dt,
           BOOL_OR(al.kind = 'nota_credito') AS has_nc
      FROM sales_payment_allocations al
      LEFT JOIN sales_payments p ON p.id = al.payment_id
      LEFT JOIN notas_credito nc ON nc.id = al.nc_id
     GROUP BY al.invoice_id
  ) sd ON sd.invoice_id = s.id
  ```
- `GET /api/ar/open-list` · `GET /api/ar/search` · `GET /api/ar/invoice/:id/payments` 응답에
  **`settled_date`(YYYY-MM-DD | null) · `settled_delay`(정수 | null) · `settled_has_nc`(bool)** 추가. 기존 필드는 무변경 = **완전 하위호환**.
- `settled_delay` 는 만기일이 없으면 `null`.

### 프런트 `refatrix-settlement.html`

- **목록에 컬럼 2개 추가**: `완납일` · `지연`. 둘 다 **헤더 클릭 정렬** 지원(3단계 토글).
  - `완납일` 첫 클릭 = 최근순, `지연` 첫 클릭 = **지연 큰 순**(가장 늦게 받은 건부터 보는 게 보통).
  - 값이 없는 미수 건은 어느 방향으로 정렬해도 **항상 맨 아래**(문자열은 빈 값, 숫자는 `-1e9`).
- **표시 규칙**: 지연 `+12일 지연`(빨강) · `정시`(초록) · `-3일 조기`(회색) · 없으면 `—`.
- **완납일 셀**: 날짜 + NC 섞였으면 「NC」 칩(툴팁: "일부가 N/C(비현금 반제)로 마감된 건입니다 — 현금 100%가 아닙니다").
- **상단 통계 2개 추가** — 완납 건이 목록에 있을 때만 표시:
  - `평균 지연 (완납 N건)` — 조기(음수)까지 포함한 산술평균, 소수 첫째 자리.
  - `정시납 (만기 내)` — 지연 ≤ 0 인 비율(%). **만기 당일은 정시로 친다.**
  - 검색 모드에서도 동일하게 나온다.
- **수금내역 드릴다운 헤더**: `완납(100%)` 칩 옆에 `완납일 2026-08-12 · 만기 2026-07-31 대비 +12일 지연`.
- **인쇄(🖨)**: 표에 완납일·지연 열 추가 + 통계 카드에 평균 지연·정시납 추가. (고객 발송용 **미수 명세서 PDF 는 무변경** — 미수 건만 담기므로 완납일이 의미 없다.)
- 완납 건을 보려면 기존처럼 **「완납(closing) 포함」 토글**을 켠다(기본은 미수만 — 수금 활동 화면을 가볍게 유지).

---

## ④ 배포

앞 작업(0191 부분배분)과 **같은 zip · 같은 순서**입니다. 별도 절차 없음:
백엔드 push → Railway Success → **`npm run migrate`**(0191) → Pages → 수금/정산 **Ctrl+Shift+R**(nav `v20260831bd`).

> 이 기능만 놓고 보면 마이그레이션이 필요 없지만, 같은 zip 의 0191 때문에 `npm run migrate` 는 반드시 실행해야 합니다.
> 백엔드 배포 전에 화면만 올라가면 완납일·지연 열이 전부 `—` 로 보일 뿐 깨지지는 않습니다.

검증:
```
curl -s ".../main/refatrix-api/src/routes/financeRoutes.js?nc=$(date +%s)" | grep -c "AR_SETTLED_SQL"   # 1+
curl -s ".../main/refatrix-settlement.html?nc=$(date +%s)" | grep -c "function arSettledDelay"          # 1+
```

## ⑤ 테스트 방법 (운영 스모크)

1. 수금/정산 → **「완납(closing) 포함: 켬」**.
2. 완납 행에 **완납일**이 찍히는지 — 부분수금이 여러 번 있었던 건은 **마지막 입금일**이어야 한다(중간 날짜 아님).
3. 그 옆 **지연**: 만기보다 늦게 받았으면 빨간 `+N일 지연`, 만기 당일이면 `정시`, 일찍 받았으면 `-N일 조기`.
4. 상단에 **평균 지연 · 정시납 %** 카드가 뜨는지.
5. **헤더 「지연」 클릭** → 가장 오래 끈 건이 위로. 다시 클릭 → 반대. 3번째 → 원래 순서. 미수 건은 항상 맨 아래.
6. NC 로 마감한 건: 완납일 옆 **「NC」 칩** + 마우스 올리면 설명 툴팁.
7. 그 행 **[수금내역]** → 헤더에 `완납일 … · 만기 … 대비 +N일 지연`.
8. **🖨 인쇄** → 완납일·지연 열과 평균 지연·정시납 카드가 인쇄본에도 나오는지.
9. **회귀**: 토글을 끄면 예전처럼 미수만 보이고, 미수 행의 완납일·지연은 `—`.

## ⑥ 검증 (이번 세션)

- `node --check` 백엔드 통과 · 변경 HTML 인라인 `<script>` 문법 검증 0 errors.
- **신규 `refatrix-api/test/ar_settled_date.test.mjs` — 14건 전부 통과**
  - **백엔드 종단(실 PG + 실 라우트) 7건**: 지연 완납(+12, 중간 부분수금일이 아님) · 조기(−3) · 정시(0) · NC 혼합(완납일=NC 적용일 8/20 · `has_nc=true`) · 미수 건은 화면에서 숨김 · 기본 목록은 완납 제외(회귀) · 검색과 드릴다운에도 동일 값 · 만기 없는 인보이스는 지연 `null`.
  - **프런트 순수 7건**: 표시 헬퍼를 `refatrix-settlement.html` 에서 **정규식으로 추출해 vm 실행**(테스트–코드 드리프트 방지) — 완납 건만 노출 / 지연·정시·조기 문구 / 색상 / NC 칩 / 통계(평균 2.0일 · 정시납 50%, 만기 당일 정시 포함).
  - 같은 DB 에 **2회 연속 재실행**해도 14/14(정리 멱등).
- **전체 스위트 회귀**: 변경 전(HEAD)·후를 **각각 새 DB**에 마이그레이션해 비교 — 실패 목록 **완전히 동일**(126건, 전부 기존 실패), **새로 깨진 테스트 0건**, 통과 270 → 297(+27 = 이번 세션 신규 두 파일).

## ⑦ 핵심 학습 / 원칙

- **"100% 들어온 날"은 MAX(반제일)** 이다. 부분수금이 섞이면 중간 날짜를 잡기 쉬운데, 잔액이 0이 되는 시점은 마지막 반제일 하나뿐이다.
- **같은 정의는 SQL 조각 하나로**(`AR_SETTLED_SQL`). 세 엔드포인트에 같은 서브쿼리를 복붙하면 반드시 한쪽만 고쳐져 어긋난다 — `AR_PAID_EPS` 때와 같은 교훈.
- **서버는 넉넉히, 화면은 엄격히.** 미수 건에도 값을 실어 보내되 표시 여부는 프런트 헬퍼 한 곳(`arSettledDate`)에서 판단하게 하면, 나중에 "부분수금 최근일"이 필요해져도 API 를 안 고쳐도 된다.
- **정직하게 드러낸다**: NC 로 마감한 건을 조용히 현금 완납처럼 보이게 하지 않고 칩으로 표시 — 반올림 잔여를 툴팁으로 노출했던 것과 같은 태도.
- **정렬 키에서 빈 값은 극값으로** 밀어 어느 방향에서도 맨 아래로 모은다(2026-06-23 세션에서 정립한 규칙 재사용).

## ⑧ 열린 메모 / 다음에 할 일

- **고객별 수금 습관 지표**: 지금은 현재 목록 범위의 평균만 낸다. 고객별/팀별 그룹 표에 「평균 지연」 열을 넣으면 수금 압박 우선순위를 바로 정할 수 있다 — 다음 후보.
- **DSO(회수기일)**: 완납일 − 발행일 기준 지표는 아직 없음. 필요하면 같은 조인으로 바로 추가 가능.
- 완납 포함 목록에 **기간 필터가 없다**(2026-06-23 메모 그대로 유효). 완납 건이 쌓이면 평균 지연이 "전체 기간 평균"이 되어 최근 추세를 못 본다 → `?closed=1&months=N` 추가 검토.
- 영업실적(`refatrix-salesperf.html`)·고객 상세(`refatrix-customers.html`) 인보이스 표에는 아직 완납일·지연이 없다. 필요하면 같은 필드를 그 API 에도 실어주면 된다.
- **`ar_paid_eps`(2026-08-27) 미반영** 경고는 여전히 유효 — 앞 인수인계 문서 상단 참조.

---
*이 문서는 인수인계용 스냅샷입니다. 큰 변경이 생기면 갱신해서 다시 올려주세요.*
