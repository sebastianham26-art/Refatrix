# REFATRIX 인수인계 — 🔗 고객 병합 (RFC 하나 · 고객번호 둘) (2026-09-03)

**마이그레이션 없음** · **환경변수 추가 없음** · **기존 API 권한 변경 없음** · nav.js 변경 없음
**산출물 zip**: `refatrix_customer_merge_v1.zip` — 백엔드 신규 1 · 수정 1 · 프런트 수정 1 · 테스트 신규 2 · 테스트 수정 1 (**6파일**)
**베이스**: 라이브 main HEAD (2026-09-03 시점) · 라이브 대비 diff **+428 / −6**, 삭제 우세 파일 0건

---

## ① 무엇을·왜

**디렉터 요청(2026-09-03)**: *"영업>고객등록 및 목록에 RFC 가 하나인데 고객번호는 나눠져 있는 경우가 있다.
C-0034 와 C-0052 가 하나로 합쳐져야 한다. C-0034 의 고객상담·방문 기록을 C-0052 로 옮겨 달라."*

0188 인수인계 ⑨ 에 「디렉터용 RFC 중복 정리 화면 — **병합**」으로 남겨 뒀던 항목이다.
지금까지는 RFC 중복을 **보여 주기만** 하고(0194 선점 예외 정리), 실제로 합칠 방법이 없었다.
그래서 「선점 보호 복구」 버튼은 진짜 중복 그룹에서는 `rfc_still_duplicated` 로 막히고,
그 상태를 풀려면 **사람이 DB 를 직접 만지는 수밖에** 없었다. 그 구멍을 화면으로 메운다.

### 확정한 방침 (디렉터 결정)

| 항목 | 결정 | 이유 |
|---|---|---|
| 복사 vs **이관** | **이관** — `customer_id` 만 바꾼다 | 사본을 만들면 AI 요약이 자동 등록한 **후속조치가 두 벌**이 되어 영업사원 화면에 같은 할 일이 두 번 뜬다. 원장은 하나여야 한다 |
| 범위 | **현장 방문 · 수기 미팅 · 고객상담** 3종 | 디렉터가 고른 범위. 견적·매출은 회계 영향이 있어 별건 |
| 견적·매출 | **옮기지 않는다** | 대신 미리보기에서 「남는 것」으로 **전부 세어** 보여 주고, 그걸 보고 확인(체크)해야 원본을 닫을 수 있다 |
| 원본 처리 | 기본 **종료(소프트삭제)**, 끌 수 있음 | 합치는 게 목적이므로 기본값은 종료. 기록만 옮기고 고객은 남기고 싶으면 체크 해제 |

### 자동으로 따라오는 것 (별도 UPDATE 없음)

`sales_visit_pendings` · `sales_visit_recordings` · `sales_consult_pendings` · `sales_consult_recordings`
는 부모 id(`visit_id`/`consult_id`)로 매달려 있어 **부모가 옮겨지면 같이 간다**.
미리보기에서는 「따라옴: 후속조치 3 · 녹음 2」로 건수만 보여 준다.

### 일부러 건드리지 않은 것

- **`place_name` · `company_name` 스냅샷은 그대로 둔다.** 그날 그 이름으로 방문한 것이 사실이고,
  지점명이 남아 있어야 나중에 왜 나뉘었는지 추적된다.
- **소프트삭제된 방문·상담도 함께 옮긴다.** 화면에는 안 보이지만, 종료된 고객에 매달아 두면 미아가 된다.
  → 그래서 안내 문구의 건수(`counts`)는 **살아있는 기록 기준**, 실제 이동 건수(`counts_all`)는 따로 돌려준다.
- **체크인이 자동 생성한 `[현장방문]` 미팅도 옮긴다.** 남기면 방문은 옮겨졌는데 그 방문의 미팅만
  옛 고객에 남아 단계 이력이 어긋난다. (고객 상세 표에서는 원래대로 이 미팅을 제외하고 보여 준다)

### 덤 — 선점 보호 자동 복구

원본이 종료되면서 남길 고객의 RFC 중복이 해소되면, **0194 재검사와 같은 규칙**으로
그 자리에서 `rfc_claim_exempt` 를 풀어 준다. 병합 직후 그 RFC 는 `uq_customers_rfc_claim` 으로 잠긴다.
(같은 RFC 를 쓰는 고객이 아직 더 있으면 풀지 않는다 — 풀면 유니크가 깨진다)

---

## ② API (신규 2 · 기존 변경 0) — 전부 `requireDirector`

**GET `/api/customers/:id/merge-preview?into=<대상 id>`**

```
{ from:{id,code,name,rfc,owner_name,team_name,approval_status,rfc_claim_exempt}, into:{…},
  same_rfc, 
  moves:[{key:'visits'|'meetings'|'consults', label, cnt, deleted,
          children:[{label,cnt}]}],
  move_total,
  residual:[{table,column,label,cnt,capped}], residual_total, residual_unavailable,
  warnings:[{code,note}], blockers:[{code,note}], can_merge }
```

- **`residual` 은 코드에 테이블을 박지 않는다.** `pg_constraint` 에서 `customers` 를 참조하는
  단일컬럼 FK 를 전부 읽어 세고, 이번에 옮기는 것만 뺀다 → **새 모듈이 생겨도 자동으로 잡힌다.**
  건수는 `LIMIT 501` 로 끊어 「500+」로 표시한다(큰 테이블에서 `count(*)` 로 끌지 않기 위해).
- `blockers` — 같은 고객 / 삭제된 고객 / 반려된 고객으로의 병합. 하나라도 있으면 실행부가 안 열린다.
- `warnings` — RFC 불일치 · 남길 고객 RFC 없음 · 승인 대기 · 팀/담당 상이. **막지 않는다**(사람이 판단).

**POST `/api/customers/merge`**
`{ from_id, into_id, close_source=true, ack_residual=false, reason }` → 단일 트랜잭션

```
{ ok, from_id, into_id, counts:{visits,meetings,consults},      // 살아있는 기록
  counts_all:{…},                                              // 소프트삭제 포함 실제 이동
  moved_total, closed, exempt_released, residual_total, note }
```

에러: `reason_required` · `residual_not_acknowledged`(409, 남는 것 목록 동봉) · `merge_blocked`(409)

트랜잭션 순서 — ① 작은 id 부터 `FOR UPDATE`(교착 방지) → ② 차단 규칙 재검사 → ③ 3종 UPDATE
→ ④ 원본 소프트삭제 → ⑤ 선점 예외 복구 → ⑥ `customer_registration_events` 에 `merge_out`/`merge_in` 두 줄.

---

## ③ 화면

**고객 상세 헤더** — `수정` 옆에 **`🔗 병합`**(디렉터에게만 보임).
**RFC 선점 이관 탭 → 🧹 선점 예외 정리** — 「실제 중복」 항목의 버튼이 `🔗 병합` 으로 바뀐다(전에는 안내만 있었다).
**고객 상세 RFC 칸** — 중복 안내 옆에도 `🔗 병합` 버튼.

모달 흐름: 옮길 고객(고정) ⇄ 남길 고객(검색·선택) → **미리보기 자동 조회** →
옮겨지는 것 / 남는 것 / 경고 → 사유 입력(필수) → (남는 것이 있으면) 확인 체크 → `🔗 병합 실행` → 확인 대화상자.
`⇄` 로 방향을 바꿀 수 있다(어느 쪽을 남길지 헷갈릴 때).

---

## ④ 배포 (마이그레이션 없음)

1. zip 6파일을 repo 루트에 풀어 덮어쓰기/추가 → Commit → Push → Railway **Success**.
2. 고객 화면 **Ctrl+Shift+R** → 탭 제목 `Refatrix ERP · 고객 등록 (merge-0903)`.
3. `npm run migrate` · 환경변수 **불필요**.

**배포 검증**
```bash
curl -s ".../main/refatrix-api/src/customerMerge.js?nc=$(date +%s)"              | grep -c "MERGE_MOVES"       # 1+
curl -s ".../main/refatrix-api/src/routes/customerRoutes.js?nc=$(date +%s)"     | grep -c "customers/merge"   # 1+
curl -s ".../main/refatrix-customers.html?nc=$(date +%s)"                       | grep -c "merge-0903"        # 1
```

---

## ⑤ C-0034 → C-0052 실제 처리 절차 (요청 건)

1. 디렉터 계정 → 영업 > **고객등록 및 목록** → `C-0034` 검색 → **[열기]**.
2. 헤더의 **`🔗 병합`** → 모달이 열리고 위쪽 빨간 칸에 `C-0034` 가 「옮길 고객」으로 잡힌다.
3. 검색창에 `C-0052` → **[이 고객으로]**.
4. 미리보기 확인 — 특히 다음 세 가지:
   - `✔ 두 고객의 RFC 가 같습니다` 가 떠 있는지 (안 뜨면 정말 같은 회사인지 다시 확인).
   - **옮겨지는 기록** 건수가 C-0034 에서 봤던 「🗣 상담·방문 이력」 건수와 맞는지.
   - **남는 것** — 견적·매출이 잡히면 그건 **옮겨지지 않는다.** 그 고객번호로 끊긴 팩투라가 있으면
     회계상 그대로 두는 게 맞고, 정리가 필요하면 별건으로 요청할 것.
5. 사유 입력: 예) `RFC FNO900101AB1 동일 — 같은 회사가 지점별로 두 번 등록됨`.
6. 남는 것이 있으면 빨간 확인란 체크 → **`🔗 병합 실행`** → 확인.
7. 실행 후 자동으로 C-0052 상세가 열린다 — 「🗣 상담·방문 이력」에 **두 고객의 기록이 합쳐져** 보이는지 확인.
8. C-0034 는 목록에서 사라진다. RFC 선점 예외도 자동으로 풀렸는지 상세의 RFC 칸에서 확인.

> ⚠ **되돌리려면 반대 방향으로 다시 병합해야 한다**(원복 버튼은 없다).
> 원본을 종료했다면 먼저 DB 에서 `deleted_at` 을 되돌려야 반대 병합이 가능하다.
> 그래서 실행 전 미리보기가 강제된 것이다.

---

## ⑥ 테스트 (운영 스모크)

1. 위 ⑤ 절차대로 C-0034 → C-0052.
2. **영업사원 계정**으로 고객 상세를 열어 `🔗 병합` 버튼이 **안 보이는지**. API 직접 호출 시 403.
3. 같은 고객끼리 병합 시도(⇄ 없이 자기 자신 선택) → 후보 목록에 자기 자신이 안 나오는지.
4. 사유를 비우고 실행 → 차단. 남는 것이 있는데 체크 없이 실행 → 차단.
5. **원본 종료 체크 해제** 후 실행 → 기록만 옮겨지고 C-0034 는 목록에 남는지(기록 0건 상태).
6. RFC 가 서로 다른 고객 둘을 골라 미리보기 → ⚠ 경고가 뜨되 실행은 되는지.
7. 병합 후 **영업활동 > 방문·동선**에서 그 방문의 후속조치·녹음 요약이 그대로 열리는지(자식이 따라왔는지).
8. 병합 후 **영업 > 고객상담** 표에서 그 상담이 C-0052 로 붙어 있는지.
9. 회귀: 상담·방문 이력 카드, 증빙서류, 배송지, 변경이력, 할인률 그래프, 엑셀 일괄등록, 등록·선점 승인.

---

## ⑦ 검증 (이 세션, 전부 통과 ✅)

**정적**
- `node --check`: `customerMerge.js` · `customerRoutes.js` · `refatrix-customers.html` 인라인 2블록 ✅
- 라이브 main 대비 diff **+428 / −6** — **삭제 우세 파일 0건**(08-24 스테일 베이스 사고 패턴 없음) ✅

**실제 PostgreSQL 16 (컨테이너 기동, 마이그레이션 0001→0194 신규 적용)**
- 잔여 FK 카탈로그 조회가 실제 스키마에서 `customers` 참조 FK 를 다수 잡아냄 ✅
- 병합 트랜잭션: `counts {visits:1, meetings:2, consults:1}` / `counts_all {visits:2,…}` —
  **소프트삭제 방문도 옮겨지고, 안내 건수는 살아있는 것만** ✅
- 후속조치·녹음이 `visit_id`/`consult_id` 로 **따라옴**(별도 UPDATE 없이) ✅
- 원본에 3종 0건 · 옮기지 않기로 한 `customer_stage_history` 는 **그대로 남음** ✅
- **남의 고객 기록 무변경** ✅
- 선점 예외 자동 복구 → 같은 RFC 신규 INSERT 가 `uq_customers_rfc_claim` 으로 **차단** ✅
- 같은 RFC 고객이 아직 더 있으면 **복구하지 않음**(유니크 보호) ✅

**실제 서버 기동 후 HTTP 통합 (12단계)**
- 미리보기 200(옮길 것·남는 것·경고 전부) ✅ / **영업사원 403** ✅ / `into` 누락 400 ✅ / 같은 고객 `can_merge:false` ✅
- 사유 없음 400 ✅ / 잔여 미확인 409(+남는 것 목록) ✅ / 실행 200 `exempt_released:true` ✅
- 병합 후 `GET /api/customers/:id/visits` 가 **합쳐진 이력**을 내려줌 ✅
- C-0034 가 목록에서 사라짐 ✅ / 선점 예외 목록 0건 ✅ / `merge_out`·`merge_in` 이력 2행 ✅

**단위/프런트 테스트**
- `customer_merge.test.mjs` **18/18** ✅ (순수 로직 16 + 실 PG 2)
- `customer_merge_front.test.mjs` **16/16** ✅ (jsdom — 모달·검색·미리보기·게이트·POST 본문·⇄·XSS)
- 회귀: `customer_visits` 28 · `customer_visits_front` 19 · `customer_visits_sql` 1 ·
  `customers_page_edit` 6 · `cross_team_request_front` 6 · `customer_registration` 68 ·
  `consult_front` 31 · `pipeline` 5 — **새로 깨진 것 0건** ✅
- `customer_registration.test.mjs` E8(빌드 마커)만 `claim-0901b` → `merge-0903` 으로 갱신(의도된 1줄 수정).

---

## ⑧ 주의 / 메모

- **원복 버튼은 없다.** 반대 방향 병합이 유일한 되돌리기이고, 원본을 종료했으면 `deleted_at` 을 먼저 되돌려야 한다.
- **견적·매출은 옮기지 않는다.** 「남는 것」에 견적·매출이 잡히는 고객을 종료하면
  그 데이터는 **종료된 고객에 붙은 채로 남는다**(조회는 되지만 고객 목록에서는 안 보인다).
  매출까지 합쳐야 하는 건은 회계 영향(원가·미수·커미션 귀속)을 정한 뒤 별건으로 붙여야 한다.
- **`customer_stage_history` 는 옮기지 않는다.** 단계 체류기간·병목 통계의 근거라,
  두 고객의 이력을 이어 붙이면 지표가 왜곡된다. 종료된 고객 쪽에 그대로 둔다.
- **커미션 귀속은 남길 고객 기준**이 된다(`rfc_claimed_by`/`owner_id`). 담당자가 다르면 미리보기에서 경고한다.
- 잔여 건수는 **501 에서 끊는다** — 「500+」로 보이면 실제로는 더 많다는 뜻.
- 병합은 `customer_registration_events` 에 `merge_out`/`merge_in` 으로 남는다.
  이 테이블에는 CHECK 제약이 없어 액션 추가에 마이그레이션이 필요 없었다.

---

## ⑨ 후속 후보

- **`customer_registration_events` 를 고객 상세의 「등록·선점 이력」 카드로 노출** — 0188·0193 부터 계속 밀린 항목.
  병합 이력(`merge_in`)이 여기 보여야 "이 고객은 언제 무엇과 합쳐졌나"를 화면에서 읽을 수 있다.
- **RFC 중복 그룹 일괄 화면** — 지금은 한 건씩 병합한다. `rfc_norm` 으로 묶어 그룹째 보여 주고
  「이 그룹을 하나로」 버튼을 붙이면 초기 정리가 빨라진다.
- **견적·매출 이관** — 회계 영향(원가 재계산·미수 재배분·커미션 귀속)을 먼저 정해야 한다. 별건.
- **병합 원복** — `merge_in` 스냅샷에 옮긴 행 id 를 남겨 두면 되돌리기가 가능해진다(현재는 건수만 남긴다).
- 병합 시 담당 영업사원에게 **WhatsApp 알림**(0188·0193 후속과 동일 항목).

*이 문서는 인수인계용 스냅샷입니다.*
